receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318
  logging:
    loglevel: info

processors:
  batch:

exporters:
  logging:
    loglevel: info

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [logging]
