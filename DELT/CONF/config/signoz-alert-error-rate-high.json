{
  "duration": "5m",
  "condition": {
    "operator": ">",
    "value": 5,
    "severity": "warning"
  },
  "query": "rate(logs{attributes.dataset=\"resonai_analytics\", attributes.level=\"ERROR\"}[5m]) / rate(logs{attributes.dataset=\"resonai_analytics\"}[5m]) * 100",
  "enabled": true,
  "annotations": {
    "summary": "Error rate exceeds 5% threshold",
    "description": "Alert triggered when error rate exceeds 5% threshold"
  },
  "name": "Error Rate High",
  "labels": {
    "component": "resonai_analytics",
    "alert_type": "reliability",
    "metric": "error_rate"
  },
  "description": "Error rate exceeds 5% threshold"
}
