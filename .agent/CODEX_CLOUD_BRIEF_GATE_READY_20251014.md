# Codex Cloud Brief — Gate Ready (2025-10-14)

Scope: Prepare/verify IONA Gate readiness across local, ci, stg, prod with evidence-to-disk and BossCat governance.

Executive Summary:
- Status inputs show READY from last run (`docs/status/tests.json`).
- Gate verification is automated via `scripts/verify-iona-gate.ps1` and CI matrix in `.github/workflows/bosscat-gate-verify.yml`.
- Required artifacts are present; prod requires queue-steward evidence before promotion.

Acceptance Criteria (IONA Gate):
- Required assets present:
  - `docs/status/tests.json`, `docs/status.html`, `CHAR/ECRR/ECRR_REPORTS`, `docs/observability/snapshots`, `docs/IONA_ERRORS.md`, `docs/cheatsheets`, `index.html`.
  - SigNoz helper present at `ALFA/TEST/helpers/signoz.ts` (OK if not under `tests/helpers/`).
  - Queue Steward evidence required for prod at `DELT/ARTF/queue-steward-verification.txt` or `artifacts/queue-steward-verification.txt`.
- Tests: In strict mode (prod) zero failing tests; non-prod may pass with warnings.
- ECRR proof written to `CHAR/ECRR/ECRR_REPORTS/` including `ECRR_GATE_RUN_LATEST.md`.

Key Inputs (as of repo state):
- Gate workflow: `.github/workflows/bosscat-gate-verify.yml:1`
- Gate script: `scripts/verify-iona-gate.ps1:1`
- Status JSON: `docs/status/tests.json:1`
- Status UI: `docs/status.html:1`
- SigNoz helper: `ALFA/TEST/helpers/signoz.ts:1`
- Queue evidence template: `artifacts/queue-steward-verification.txt:1`
- BossCat guide: `docs/BossCat/README.md:1`

Runbook (Local First):
1) Preflight and install
   - Ensure Node 20+, pnpm installed.
   - `pnpm install`
2) Gate verify (site-specific)
   - Default: `pnpm run agent:ready-for-gate`
   - Local: `pnpm run agent:ready-for-gate:local`
   - CI: `pnpm run agent:ready-for-gate:ci`
   - STG: `pnpm run agent:ready-for-gate:stg`
   - PROD (strict): `pnpm run agent:ready-for-gate:prod`
3) Outputs (proof-to-disk)
   - `DELT/ARTF/gate-verification-results.json` — machine verdict
   - `CHAR/ECRR/ECRR_REPORTS/ECRR_GATE_RUN_*.md` + `ECRR_GATE_RUN_LATEST.md` — ECRR
   - `PR_COMMENT_IONA_GATE_002_FINAL.md` — PR comment payload
4) Nightly dashboards (optional now, required for cadence)
   - `pnpm run export:signoz:playwright`
   - Artifacts under `docs/observability/snapshots/`

Prod-Specific Requirements:
- Queue Steward evidence file must be populated (not just template):
  - Path: `DELT/ARTF/queue-steward-verification.txt` or `artifacts/queue-steward-verification.txt`.
  - Include run ID, timestamps, queue depth, blockers, and rollback link to latest ECRR.
- SBOM (toggle-based strictness handled by workflow). Strict enforcement controlled by `SBOM_STRICT` repo/org variable.

ECRR Evidence Model (generated by gate script):
- Examine: Lists present/missing assets and test summary.
- Report: `Gate Verdict: READY|READY_WITH_WARNINGS|NOT_READY` with reasons.
- Artifacts stored to `CHAR/ECRR/ECRR_REPORTS/` and mirrored as LATEST.

What Codex Cloud Should Do Now:
- Investigator
  - Run `pnpm run agent:ready-for-gate:ci` and review `DELT/ARTF/gate-verification-results.json`.
  - Confirm SigNoz helper reachable and status page loads locally.
- Gap-Closer
  - If any required paths are missing, create minimal stubs and wire nightly export.
  - For prod, fill `queue-steward-verification.txt` with current run data before promotion.
- QA Scribe
  - Ensure ECRR gate report exists and links from `docs/status.html` resolve.
  - Optionally run `scripts/benchmark-process-all-ecrr-reports.ps1` to refresh `DELT/ARTF/ecrr-benchmark.json`.

CI Matrix Expectations:
- Sites: local, ci, stg, prod. Gate: IONA.
- Non-prod may return READY_WITH_WARNINGS if tests fail; prod strict returns NOT_READY when assets missing or tests fail.

Checklist (Gate Ready 2025-10-14):
- [ ] CI/local gate run executed and READY.
- [ ] ECRR report updated: `CHAR/ECRR/ECRR_REPORTS/ECRR_GATE_RUN_LATEST.md`.
- [ ] PR comment prepared: `PR_COMMENT_IONA_GATE_002_FINAL.md`.
- [ ] Nightly dashboard export validated: `docs/observability/snapshots/` updated.
- [ ] PROD: `queue-steward-verification.txt` completed with real values.
- [ ] SBOM policy noted (strict toggle via `SBOM_STRICT`).

Notes:
- Current `docs/status/tests.json` shows READY with evidence collected on 2025-10-10; rerun gate to refresh timestamps and commit metadata.
- SigNoz UI endpoint default: `http://localhost:8080`; OTLP ports: 5317 (gRPC), 5318 (HTTP).


