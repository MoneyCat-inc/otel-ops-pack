# GPU-accelerated PyTorch dependencies
# Use this file when GPU support is required (CUDA 12.1)
# 
# Installation instructions:
# pip install -r requirements-gpu.txt -f https://download.pytorch.org/whl/torch_stable.html
#
# Note: These CUDA-specific builds require:
# - Compatible CUDA drivers (12.1+)
# - Python >=3.8,<3.12 (CUDA builds may not support Python 3.12+)
# - Appropriate hardware (NVIDIA GPU with CUDA support)

filelock==3.19.1
fsspec==2024.6.1
Jinja2==3.1.6
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.5
numpy==2.3.3
pillow==11.0.0

# CUDA-accelerated PyTorch packages (requires special index)
torch==2.8.0
torchaudio==2.5.1+cu121
torchvision==0.20.1+cu121

typing_extensions==4.15.0
