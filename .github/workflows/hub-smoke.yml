name: Hub Uptime Smoke
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Check critical endpoints
        run: |
          set -euo pipefail
          urls=(
            "https://hub.resonai.io/"
            "https://hub.resonai.io/assets/hub.js"
            "https://hub.resonai.io/docs/status/kpis.json"
            "https://hub.resonai.io/docs/status.html"
            "https://hub.resonai.io/docs/dashboards/live-metrics.html"
            "https://hub.resonai.io/docs/BossCat/data_room_enhanced.html"
          )
          for u in "${urls[@]}"; do
            code=$(curl -s -o /dev/null -w "%{http_code}" "$u")
            [ "$code" = "200" ] && echo "OK $u" || { echo "::error ::$u returned $code"; exit 1; }
          done

