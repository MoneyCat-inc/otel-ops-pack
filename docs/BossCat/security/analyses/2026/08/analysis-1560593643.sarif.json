{"runs":[{"artifacts":[{"location":{"index":0,"uri":".github/workflows/boss-gate-verify.yml"}},{"location":{"index":1,"uri":".github/workflows/boss-gate-signal-and-merge.yml"}},{"location":{"index":2,"uri":".github/workflows/bosscat-diagnostic.yml"}},{"location":{"index":3,"uri":".github/workflows/bosscat-regression-matrix.yml"}},{"location":{"index":4,"uri":".github/workflows/bosscat-tetragram-guard.yml"}},{"location":{"index":5,"uri":".github/workflows/ci-disabled.yml"}},{"location":{"index":6,"uri":".github/workflows/docs-guard.yml"}},{"location":{"index":7,"uri":".github/workflows/ecrr-compliance.yml"}},{"location":{"index":8,"uri":".github/workflows/ecrr-compliance-scheduled.yml"}},{"location":{"index":9,"uri":".github/workflows/gate-026-performance.yml"}},{"location":{"index":10,"uri":".github/workflows/gate-verify.yml"}},{"location":{"index":11,"uri":".github/workflows/gitleaks.yml"}},{"location":{"index":12,"uri":".github/workflows/hub-smoke.yml"}},{"location":{"index":13,"uri":".github/workflows/guardrails.yml"}},{"location":{"index":14,"uri":".github/workflows/link-check.yml"}},{"location":{"index":15,"uri":".github/workflows/k6-performance-gate.yml"}},{"location":{"index":16,"uri":".github/workflows/multi-app-ci.yml"}},{"location":{"index":17,"uri":".github/workflows/nightly-dashboard-reports.yml"}},{"location":{"index":18,"uri":".github/workflows/nightly-ecrr-aggregates.yml"}},{"location":{"index":19,"uri":".github/workflows/nightly-reference-map.yml"}},{"location":{"index":20,"uri":".github/workflows/nightly-tetragrammaton-benchmarks.yml"}},{"location":{"index":21,"uri":".github/workflows/performance-gate.yml"}},{"location":{"index":22,"uri":".github/workflows/registry-guard.yml"}},{"location":{"index":23,"uri":".github/workflows/perf-gate-demo.yml"}},{"location":{"index":24,"uri":".github/workflows/repository-security-check.yml"}},{"location":{"index":25,"uri":".github/workflows/rsi-gate.yml"}},{"location":{"index":26,"uri":".github/workflows/rsi-archive.yml"}},{"location":{"index":27,"uri":".github/workflows/rsi-index.yml"}},{"location":{"index":28,"uri":".github/workflows/rsi-sweep-nightly.yml"}},{"location":{"index":29,"uri":".github/workflows/signoz-automation-fresh.yml"}},{"location":{"index":30,"uri":".github/workflows/stress-test-nightly.yml"}},{"location":{"index":31,"uri":".github/workflows/stress-test-pr.yml"}},{"location":{"index":32,"uri":".github/workflows/weekly-cleanup.yml"}},{"location":{"index":33,"uri":".github/workflows/weekly-executive-review.yml"}}],"automationDetails":{"id":"/language:actions/"},"conversion":{"tool":{"driver":{"name":"GitHub Code Scanning"}}},"properties":{"codeqlConfigSummary":{}},"results":[{"correlationGuid":"e18a34da-01dc-4801-b941-01c98053c9e3","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":0,"uri":".github/workflows/boss-gate-verify.yml"},"region":{"endColumn":39,"endLine":181,"startColumn":5,"startLine":20}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"c40b492690d2d2a9:1"},"properties":{"github/alertNumber":9886,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/9886"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"e0cb891d-550d-4f6c-853e-c308e43eef4c","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":1,"uri":".github/workflows/boss-gate-signal-and-merge.yml"},"region":{"endColumn":3,"endLine":93,"startColumn":5,"startLine":28}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"51c80cf3d114c551:1"},"properties":{"github/alertNumber":2256,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/2256"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"06621b6c-7fca-4954-a052-982929470a2e","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":1,"uri":".github/workflows/boss-gate-signal-and-merge.yml"},"region":{"endColumn":74,"endLine":125,"startColumn":5,"startLine":94}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"cd6447dcf1ae39c6:1"},"properties":{"github/alertNumber":2257,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/2257"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"cf6bb1ad-6cfc-4d92-85f4-2323d812050b","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":2,"uri":".github/workflows/bosscat-diagnostic.yml"},"region":{"endColumn":67,"endLine":124,"startColumn":5,"startLine":18}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"e0c7f0ee5eb96ea8:1"},"properties":{"github/alertNumber":2596,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/2596"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"34a5f412-c268-4d12-8828-0df096054e46","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":3,"uri":".github/workflows/bosscat-regression-matrix.yml"},"region":{"endColumn":28,"endLine":179,"startColumn":5,"startLine":23}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"651bf766335a2108:1"},"properties":{"github/alertNumber":9891,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/9891"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"4a9b65b5-b909-476a-83b5-7a31b3bc2bfe","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":4,"uri":".github/workflows/bosscat-tetragram-guard.yml"},"region":{"endColumn":46,"endLine":29,"startColumn":5,"startLine":14}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"59a6807acf59ba2b:1"},"properties":{"github/alertNumber":10327,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10327"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"a1a00c4e-eaec-4e1d-bce5-5eb34048231c","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":5,"uri":".github/workflows/ci-disabled.yml"},"region":{"endColumn":3,"endLine":65,"startColumn":5,"startLine":23}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"8304c81c32ffc951:1"},"properties":{"github/alertNumber":2258,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/2258"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"1a19ead2-2f2d-4b49-9ce4-f2eef21d3f65","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":5,"uri":".github/workflows/ci-disabled.yml"},"region":{"endColumn":3,"endLine":105,"startColumn":5,"startLine":66}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"a33272b308920c20:1"},"properties":{"github/alertNumber":2259,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/2259"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"d3f21ede-1883-4a45-88c3-251af20f9a4d","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":5,"uri":".github/workflows/ci-disabled.yml"},"region":{"endColumn":68,"endLine":123,"startColumn":5,"startLine":106}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{}}"},"partialFingerprints":{"primaryLocationLineHash":"5e205984b8be492d:1"},"properties":{"github/alertNumber":2260,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/2260"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"54cd356c-e573-4525-aea1-c2b4b80aa595","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":6,"uri":".github/workflows/docs-guard.yml"},"region":{"endColumn":78,"endLine":86,"startColumn":5,"startLine":18}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"847fee94423792c6:1"},"properties":{"github/alertNumber":10904,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10904"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"f12b4621-3210-4e08-922f-d3e65551b5bc","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":7,"uri":".github/workflows/ecrr-compliance.yml"},"region":{"endColumn":16,"endLine":72,"startColumn":5,"startLine":30}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"da09ebb3438dece5:1"},"properties":{"github/alertNumber":10905,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10905"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"44ef6d38-20af-4329-a4b4-543dc065d727","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":8,"uri":".github/workflows/ecrr-compliance-scheduled.yml"},"region":{"endColumn":15,"endLine":80,"startColumn":5,"startLine":15}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"8e6e20ef0f0f760d:1"},"properties":{"github/alertNumber":10745,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10745"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"3a3a00a0-c457-4596-a8d8-36c43c02f882","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":9,"uri":".github/workflows/gate-026-performance.yml"},"region":{"endColumn":21,"endLine":198,"startColumn":5,"startLine":33}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"42ed7e6534aaace5:1"},"properties":{"github/alertNumber":10581,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10581"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"2f7eb49e-5a6c-4c15-874f-5b06c099f7c0","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":10,"uri":".github/workflows/gate-verify.yml"},"region":{"endColumn":3,"endLine":189,"startColumn":5,"startLine":9}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"99c6a07111c665db:1"},"properties":{"github/alertNumber":9898,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/9898"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"e8757c75-b860-42ee-813e-e24a40d59cb8","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":11,"uri":".github/workflows/gitleaks.yml"},"region":{"endColumn":30,"endLine":55,"startColumn":5,"startLine":24}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"88e1665e063ec2d5:1"},"properties":{"github/alertNumber":11169,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/11169"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"a56023d5-f290-4446-847d-6db68df52e1a","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":10,"uri":".github/workflows/gate-verify.yml"},"region":{"endColumn":50,"endLine":195,"startColumn":5,"startLine":190}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"588d74ce8f173b8a:1"},"properties":{"github/alertNumber":9899,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/9899"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"a67a504a-ec99-43b5-a9cb-f85e02f907a9","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":12,"uri":".github/workflows/hub-smoke.yml"},"region":{"endColumn":16,"endLine":27,"startColumn":5,"startLine":9}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{}}"},"partialFingerprints":{"primaryLocationLineHash":"bbe68ca2a286119f:1"},"properties":{"github/alertNumber":10378,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10378"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"6ede36b1-a046-4b22-a5b9-44a865ccc725","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":13,"uri":".github/workflows/guardrails.yml"},"region":{"endColumn":94,"endLine":70,"startColumn":5,"startLine":15}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"fd2b6cd5fbf8e569:1"},"properties":{"github/alertNumber":10328,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10328"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"3976cbc1-0008-4484-896d-e2854101ad29","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":14,"uri":".github/workflows/link-check.yml"},"region":{"endColumn":51,"endLine":16,"startColumn":5,"startLine":9}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"f20a075b2a2ab9a3:1"},"properties":{"github/alertNumber":10379,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10379"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"7c63da7c-80cc-40b1-a399-ff0dcfa5bfe4","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":15,"uri":".github/workflows/k6-performance-gate.yml"},"region":{"endColumn":14,"endLine":84,"startColumn":5,"startLine":19}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"c99c8a95a790985a:1"},"properties":{"github/alertNumber":10552,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10552"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"5b16ebb8-d267-405e-bee5-db91dd8f7770","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":16,"uri":".github/workflows/multi-app-ci.yml"},"region":{"endColumn":3,"endLine":80,"startColumn":5,"startLine":30}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"2c8d3f364332b0d6:1"},"properties":{"github/alertNumber":9827,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/9827"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"7b33d68e-c452-4db7-809d-5fcb277f3830","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":16,"uri":".github/workflows/multi-app-ci.yml"},"region":{"endColumn":3,"endLine":138,"startColumn":5,"startLine":81}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"5f96e88a4ec1f0ba:1"},"properties":{"github/alertNumber":9828,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/9828"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"ab92e137-e1bc-490b-bed0-fd5033401613","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":17,"uri":".github/workflows/nightly-dashboard-reports.yml"},"region":{"endColumn":85,"endLine":197,"startColumn":5,"startLine":24}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"9b4a9b5583e670c2:1"},"properties":{"github/alertNumber":11113,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/11113"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"bfcb171d-db0d-4c76-b1c3-f3d638cd73c2","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":18,"uri":".github/workflows/nightly-ecrr-aggregates.yml"},"region":{"endColumn":30,"endLine":36,"startColumn":5,"startLine":10}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"641221cbe182ef62:1"},"properties":{"github/alertNumber":9881,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/9881"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"db196f05-2d54-4898-9f34-98f5edfe4b2d","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":16,"uri":".github/workflows/multi-app-ci.yml"},"region":{"endColumn":3,"endLine":191,"startColumn":5,"startLine":171}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"a9d0ee7a61b28952:1"},"properties":{"github/alertNumber":11112,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/11112"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"84f44ebb-c930-404f-ac8a-3f4853e64b1d","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":16,"uri":".github/workflows/multi-app-ci.yml"},"region":{"endColumn":14,"endLine":208,"startColumn":5,"startLine":192}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{}}"},"partialFingerprints":{"primaryLocationLineHash":"8e05a8a569f052fa:1"},"properties":{"github/alertNumber":9830,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/9830"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"6f336823-2a7a-4600-8aaa-68c0601bb1f2","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":19,"uri":".github/workflows/nightly-reference-map.yml"},"region":{"endColumn":30,"endLine":34,"startColumn":5,"startLine":10}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"641221cbe182ef62:1"},"properties":{"github/alertNumber":9883,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/9883"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"d50b4980-57d2-4ce2-adbe-07a6277d1573","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":20,"uri":".github/workflows/nightly-tetragrammaton-benchmarks.yml"},"region":{"endColumn":5,"endLine":39,"startColumn":5,"startLine":39}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"60dac88beceb57c8:1"},"properties":{"github/alertNumber":2571,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/2571"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"1c4377b5-1428-4f60-b033-6a89aeba81ee","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":21,"uri":".github/workflows/performance-gate.yml"},"region":{"endColumn":39,"endLine":97,"startColumn":5,"startLine":22}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"d6459876e9f05c18:1"},"properties":{"github/alertNumber":10906,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10906"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"fbf91105-64ae-43c9-8ee4-03d120b73fe7","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":22,"uri":".github/workflows/registry-guard.yml"},"region":{"endColumn":39,"endLine":151,"startColumn":5,"startLine":20}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"e57a176ef045d16a:1"},"properties":{"github/alertNumber":10381,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10381"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"b3cd05ee-d794-4279-adb2-a2871bb82fe0","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":23,"uri":".github/workflows/perf-gate-demo.yml"},"region":{"endColumn":18,"endLine":145,"startColumn":5,"startLine":23}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"117370af2c36c486:1"},"properties":{"github/alertNumber":10676,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10676"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"b70d2cd4-acdf-48dd-83f2-ab0c7a060c10","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":24,"uri":".github/workflows/repository-security-check.yml"},"region":{"endColumn":5,"endLine":16,"startColumn":5,"startLine":16}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"e96ac3e502973da9:1"},"properties":{"github/alertNumber":355,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/355"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"c96f6121-990f-4e76-860b-43b88668e1c4","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":25,"uri":".github/workflows/rsi-gate.yml"},"region":{"endColumn":55,"endLine":38,"startColumn":5,"startLine":16}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"59a6807acf59ba2b:1"},"properties":{"github/alertNumber":10329,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10329"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"751e119a-df85-4865-b534-3b048beb32ff","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":26,"uri":".github/workflows/rsi-archive.yml"},"region":{"endColumn":57,"endLine":23,"startColumn":5,"startLine":8}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"7b25e92bc5107881:1"},"properties":{"github/alertNumber":10320,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10320"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"ef7ed2cc-82f1-471d-a2d4-4dab1bc31f76","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":27,"uri":".github/workflows/rsi-index.yml"},"region":{"endColumn":55,"endLine":27,"startColumn":5,"startLine":8}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"7b25e92bc5107881:1"},"properties":{"github/alertNumber":10322,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10322"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"b3369593-ea0a-403d-b5ba-ea802f761022","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":28,"uri":".github/workflows/rsi-sweep-nightly.yml"},"region":{"endColumn":119,"endLine":85,"startColumn":5,"startLine":10}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"3162b96e9742d92a:1"},"properties":{"github/alertNumber":10325,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10325"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"5b59d8c2-6ab6-4311-90d9-59e96e7526f0","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":29,"uri":".github/workflows/signoz-automation-fresh.yml"},"region":{"endColumn":3,"endLine":56,"startColumn":5,"startLine":35}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{}}"},"partialFingerprints":{"primaryLocationLineHash":"6006a47c0be14595:1"},"properties":{"github/alertNumber":11,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/11"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"3a063c74-b3b8-46d9-961d-b9d66016a046","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":29,"uri":".github/workflows/signoz-automation-fresh.yml"},"region":{"endColumn":3,"endLine":187,"startColumn":5,"startLine":57}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"396f7b0df4ce829a:1"},"properties":{"github/alertNumber":12,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/12"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"2467cba2-6836-40fa-a040-460f8b3f3a95","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":29,"uri":".github/workflows/signoz-automation-fresh.yml"},"region":{"endColumn":47,"endLine":204,"startColumn":5,"startLine":188}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{}}"},"partialFingerprints":{"primaryLocationLineHash":"396f7b0df330d3e2:1"},"properties":{"github/alertNumber":13,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/13"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"fe3760ba-594a-4d1e-ab6c-f6cae0ec0011","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":30,"uri":".github/workflows/stress-test-nightly.yml"},"region":{"endColumn":51,"endLine":150,"startColumn":5,"startLine":27}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"8b1a117fc682f131:1"},"properties":{"github/alertNumber":5081,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/5081"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"3a453250-53bc-4b87-b5fb-a9265aa1b28a","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":31,"uri":".github/workflows/stress-test-pr.yml"},"region":{"endColumn":5,"endLine":20,"startColumn":5,"startLine":20}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"8ddc6fca3584a0b6:1"},"properties":{"github/alertNumber":9888,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/9888"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"90616878-0792-494d-a329-0adb0bc9b945","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":32,"uri":".github/workflows/weekly-cleanup.yml"},"region":{"endColumn":30,"endLine":56,"startColumn":5,"startLine":15}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"d229b18ccc83c01e:1"},"properties":{"github/alertNumber":10748,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10748"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"},{"correlationGuid":"45b77fe1-e845-41d2-ae59-251664feb896","level":"warning","locations":[{"physicalLocation":{"artifactLocation":{"index":33,"uri":".github/workflows/weekly-executive-review.yml"},"region":{"endColumn":30,"endLine":161,"startColumn":5,"startLine":24}}}],"message":{"text":"Actions job or workflow does not limit the permissions of the GITHUB_TOKEN. Consider setting an explicit permissions block, using the following as a minimal starting point: {{contents: read}}"},"partialFingerprints":{"primaryLocationLineHash":"7bd430062d96fd27:1"},"properties":{"github/alertNumber":10747,"github/alertUrl":"https://api.github.com/repos/MoneyCat-inc/otel-ops-pack/code-scanning/alerts/10747"},"rule":{"id":"actions/missing-workflow-permissions","toolComponent":{"index":0},"index":9},"ruleId":"actions/missing-workflow-permissions"}],"tool":{"driver":{"name":"CodeQL","semanticVersion":"2.26.2"},"extensions":[{"name":"codeql/actions-queries","rules":[{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"An attacker may be able to poison the workflow's artifacts and influence on consequent steps."},"help":{"markdown":"## Overview\n\nThe workflow downloads artifacts that may be poisoned by an attacker in previously triggered workflows. If the contents of these artifacts are not correctly extracted, stored and verified, they may lead to repository compromise if untrusted code gets executed in a privileged job.\n\n## Recommendation\n\n- Always consider artifacts content as untrusted.\n- Extract the contents of artifacts to a temporary folder so they cannot override existing files.\n- Verify the contents of the artifacts downloaded. If an artifact is expected to contain a numeric value, verify it before using it.\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow downloads an artifact that can potentially be controlled by an attacker and then runs a script from the runner workspace. Because the `dawidd6/action-download-artifact` by default downloads and extracts the contents of the artifacts overriding existing files, an attacker will be able to override the contents of `cmd.sh` and gain code execution when this file gets executed.\n\n```yaml\nname: Insecure Workflow\n\non:\n  workflow_run:\n    workflows: [\"Prev\"]\n    types:\n      - completed\n\njobs:\n  Download:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n      - uses: dawidd6/action-download-artifact@v2\n        with:\n          name: pr_number\n      - name: Run command\n        run: |\n          sh cmd.sh\n```\n\n### Correct Usage\n\nThe following example, correctly creates a temporary directory and extracts the contents of the artifact there before calling `cmd.sh`.\n\n```yaml\nname: Secure Workflow\n\non:\n  workflow_run:\n    workflows: [\"Prev\"]\n    types:\n      - completed\n\njobs:\n  Download:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n      - run: mkdir -p ${{ runner.temp }}/artifacts/\n      - uses: dawidd6/action-download-artifact@v2\n        with:\n          name: pr_number\n          path: ${{ runner.temp }}/artifacts/\n\n      - name: Run command\n        run: |\n          sh cmd.sh\n```\n\n## References\n\n- GitHub Security Lab Research: [Keeping your GitHub Actions and workflows secure Part 1: Preventing pwn requests](https://securitylab.github.com/research/github-actions-preventing-pwn-requests/).\n","text":"## Overview\n\nThe workflow downloads artifacts that may be poisoned by an attacker in previously triggered workflows. If the contents of these artifacts are not correctly extracted, stored and verified, they may lead to repository compromise if untrusted code gets executed in a privileged job.\n\n## Recommendation\n\n- Always consider artifacts content as untrusted.\n- Extract the contents of artifacts to a temporary folder so they cannot override existing files.\n- Verify the contents of the artifacts downloaded. If an artifact is expected to contain a numeric value, verify it before using it.\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow downloads an artifact that can potentially be controlled by an attacker and then runs a script from the runner workspace. Because the `dawidd6/action-download-artifact` by default downloads and extracts the contents of the artifacts overriding existing files, an attacker will be able to override the contents of `cmd.sh` and gain code execution when this file gets executed.\n\n```yaml\nname: Insecure Workflow\n\non:\n  workflow_run:\n    workflows: [\"Prev\"]\n    types:\n      - completed\n\njobs:\n  Download:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n      - uses: dawidd6/action-download-artifact@v2\n        with:\n          name: pr_number\n      - name: Run command\n        run: |\n          sh cmd.sh\n```\n\n### Correct Usage\n\nThe following example, correctly creates a temporary directory and extracts the contents of the artifact there before calling `cmd.sh`.\n\n```yaml\nname: Secure Workflow\n\non:\n  workflow_run:\n    workflows: [\"Prev\"]\n    types:\n      - completed\n\njobs:\n  Download:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n      - run: mkdir -p ${{ runner.temp }}/artifacts/\n      - uses: dawidd6/action-download-artifact@v2\n        with:\n          name: pr_number\n          path: ${{ runner.temp }}/artifacts/\n\n      - name: Run command\n        run: |\n          sh cmd.sh\n```\n\n## References\n\n- GitHub Security Lab Research: [Keeping your GitHub Actions and workflows secure Part 1: Preventing pwn requests](https://securitylab.github.com/research/github-actions-preventing-pwn-requests/).\n"},"id":"actions/artifact-poisoning/critical","name":"actions/artifact-poisoning/critical","properties":{"precision":"very-high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-829/ArtifactPoisoningCritical.ql","security-severity":"9","tags":["actions","external/cwe/cwe-829","security"]},"shortDescription":{"text":"Artifact poisoning"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"The cache can be poisoned by untrusted code, leading to a cache poisoning attack."},"help":{"markdown":"## Overview\n\nGitHub Actions cache poisoning is a technique that allows an attacker to inject malicious content into the Action's cache from unprivileged workflow, potentially leading to code execution in privileged workflows.\n\nAn attacker with the ability to run code in the context of the default branch (e.g. through Code Injection or Execution of Untrusted Code) can exploit this to:\n\n1. Steal the cache access token and URL.\n2. Overflow the cache to trigger eviction of legitimate entries.\n3. Poison cache entries with malicious payloads.\n4. Achieve code execution in privileged workflows that restore the poisoned cache.\n\nThis allows lateral movement from low-privileged to high-privileged workflows within a repository.\n\n### Cache Structure\n\nIn GitHub Actions, cache scopes are primarily determined by the branch structure. Branches are considered the main security boundary for GitHub Actions caching. This means that cache entries are generally scoped to specific branches.\n\n- **Access to Parent Branch Caches**: Feature branches (or child branches) created off of a parent branch (like `main` or `dev`) can access caches from the parent branch. For instance, a feature branch off of `main` will be able to access the cache from `main`.\n\n- **Sibling Branches**: Sibling branches, meaning branches that are created from the same parent but not from each other, do not share caches. For example, two branches created off of `main` will not be able to access each other’s caches directly.\n\nDue to the above design, if something is cached in the context of the default branch (e.g., `main`), it becomes accessible to any feature branch derived from `main`.\n\n## Recommendation\n\n1. Avoid using caching in workflows that handle sensitive operations like releases.\n2. If caching must be used:\n   - Validate restored cache contents before use.\n   - Use short-lived, workflow-specific cache keys.\n   - Clear caches regularly.\n3. Implement strict isolation between untrusted and privileged workflow execution.\n4. Never run untrusted code in the context of the default branch.\n5. Sign the cache value cryptographically and verify the signature before usage.\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow is vulnerable to code injection in a non-privileged job but in the context of the default branch.\n\n```yaml\nname: Vulnerable Workflow\non:\n  issue_comment:\n    types: [created]\n\njobs:\n  pr-comment:\n    permissions: {}\n    runs-on: ubuntu-latest\n    steps:\n      - run: |\n          echo ${{ github.event.comment.body }}\n```\n\n### Correct Usage\n\nThe following workflow is not vulnerable to code injections even if it runs in the context of the default branch.\n\n```yaml\nname: Secure Workflow\non:\n  issue_comment:\n    types: [created]\n\njobs:\n  pr-comment:\n    permissions: {}\n    runs-on: ubuntu-latest\n    steps:\n      - env:\n          BODY: ${{ github.event.comment.body }}\n        run: |\n          echo \"$BODY\"\n```\n\n## References\n\n- Adnan Khan's Blog: [The Monsters in Your Build Cache – GitHub Actions Cache Poisoning](https://adnanthekhan.com/2024/05/06/the-monsters-in-your-build-cache-github-actions-cache-poisoning/).\n- GitHub Docs: [GitHub Actions Caching Documentation](https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows).\n- Scribe Security Blog: [Cache Poisoning in GitHub Actions](https://scribesecurity.com/blog/github-cache-poisoning/).\n","text":"## Overview\n\nGitHub Actions cache poisoning is a technique that allows an attacker to inject malicious content into the Action's cache from unprivileged workflow, potentially leading to code execution in privileged workflows.\n\nAn attacker with the ability to run code in the context of the default branch (e.g. through Code Injection or Execution of Untrusted Code) can exploit this to:\n\n1. Steal the cache access token and URL.\n2. Overflow the cache to trigger eviction of legitimate entries.\n3. Poison cache entries with malicious payloads.\n4. Achieve code execution in privileged workflows that restore the poisoned cache.\n\nThis allows lateral movement from low-privileged to high-privileged workflows within a repository.\n\n### Cache Structure\n\nIn GitHub Actions, cache scopes are primarily determined by the branch structure. Branches are considered the main security boundary for GitHub Actions caching. This means that cache entries are generally scoped to specific branches.\n\n- **Access to Parent Branch Caches**: Feature branches (or child branches) created off of a parent branch (like `main` or `dev`) can access caches from the parent branch. For instance, a feature branch off of `main` will be able to access the cache from `main`.\n\n- **Sibling Branches**: Sibling branches, meaning branches that are created from the same parent but not from each other, do not share caches. For example, two branches created off of `main` will not be able to access each other’s caches directly.\n\nDue to the above design, if something is cached in the context of the default branch (e.g., `main`), it becomes accessible to any feature branch derived from `main`.\n\n## Recommendation\n\n1. Avoid using caching in workflows that handle sensitive operations like releases.\n2. If caching must be used:\n   - Validate restored cache contents before use.\n   - Use short-lived, workflow-specific cache keys.\n   - Clear caches regularly.\n3. Implement strict isolation between untrusted and privileged workflow execution.\n4. Never run untrusted code in the context of the default branch.\n5. Sign the cache value cryptographically and verify the signature before usage.\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow is vulnerable to code injection in a non-privileged job but in the context of the default branch.\n\n```yaml\nname: Vulnerable Workflow\non:\n  issue_comment:\n    types: [created]\n\njobs:\n  pr-comment:\n    permissions: {}\n    runs-on: ubuntu-latest\n    steps:\n      - run: |\n          echo ${{ github.event.comment.body }}\n```\n\n### Correct Usage\n\nThe following workflow is not vulnerable to code injections even if it runs in the context of the default branch.\n\n```yaml\nname: Secure Workflow\non:\n  issue_comment:\n    types: [created]\n\njobs:\n  pr-comment:\n    permissions: {}\n    runs-on: ubuntu-latest\n    steps:\n      - env:\n          BODY: ${{ github.event.comment.body }}\n        run: |\n          echo \"$BODY\"\n```\n\n## References\n\n- Adnan Khan's Blog: [The Monsters in Your Build Cache – GitHub Actions Cache Poisoning](https://adnanthekhan.com/2024/05/06/the-monsters-in-your-build-cache-github-actions-cache-poisoning/).\n- GitHub Docs: [GitHub Actions Caching Documentation](https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows).\n- Scribe Security Blog: [Cache Poisoning in GitHub Actions](https://scribesecurity.com/blog/github-cache-poisoning/).\n"},"id":"actions/cache-poisoning/code-injection","name":"actions/cache-poisoning/code-injection","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-349/CachePoisoningViaCodeInjection.ql","security-severity":"7.5","tags":["actions","external/cwe/cwe-094","external/cwe/cwe-349","security"]},"shortDescription":{"text":"Cache Poisoning via low-privileged code injection"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"The cache can be poisoned by untrusted code, leading to a cache poisoning attack."},"help":{"markdown":"## Overview\n\nGitHub Actions cache poisoning is a technique that allows an attacker to inject malicious content into the Action's cache from unprivileged workflow, potentially leading to code execution in privileged workflows.\n\nAn attacker with the ability to run code in the context of the default branch (e.g. through Code Injection or Execution of Untrusted Code) can exploit this to:\n\n1. Steal the cache access token and URL.\n2. Overflow the cache to trigger eviction of legitimate entries.\n3. Poison cache entries with malicious payloads.\n4. Achieve code execution in privileged workflows that restore the poisoned cache.\n\nThis allows lateral movement from low-privileged to high-privileged workflows within a repository.\n\n### Cache Structure\n\nIn GitHub Actions, cache scopes are primarily determined by the branch structure. Branches are considered the main security boundary for GitHub Actions caching. This means that cache entries are generally scoped to specific branches.\n\n- **Access to Parent Branch Caches**: Feature branches (or child branches) created off of a parent branch (like `main` or `dev`) can access caches from the parent branch. For instance, a feature branch off of `main` will be able to access the cache from `main`.\n\n- **Sibling Branches**: Sibling branches, meaning branches that are created from the same parent but not from each other, do not share caches. For example, two branches created off of `main` will not be able to access each other’s caches directly.\n\nDue to the above design, if something is cached in the context of the default branch (e.g., `main`), it becomes accessible to any feature branch derived from `main`.\n\n## Recommendation\n\n1. Avoid using caching in workflows that handle sensitive operations like releases.\n2. If caching must be used:\n   - Validate restored cache contents before use.\n   - Use short-lived, workflow-specific cache keys.\n   - Clear caches regularly.\n3. Implement strict isolation between untrusted and privileged workflow execution.\n4. Never run untrusted code in the context of the default branch.\n5. Sign the cache value cryptographically and verify the signature before usage.\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow is caching an attacker-controlled file (`large_file`) in the context of the default branch.\n\n```yaml\nname: Vulnerable Workflow\non:\n  issue_comment:\n    types: [created]\n\njobs:\n  pr-comment:\n    permissions: read-all\n    runs-on: ubuntu-latest\n    steps:\n      - uses: xt0rted/pull-request-comment-branch@v2\n        id: comment-branch\n      - uses: actions/checkout@v3\n        with:\n          ref: ${{ steps.comment-branch.outputs.head_sha }}\n      - name: Set up Python 3.10\n        uses: actions/setup-python@v5\n      - name: Cache pip dependencies\n        uses: actions/cache@v4\n        id: cache-pip\n        with:\n          path: ~/.cache/pip\n          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}\n          restore-keys: ${{ runner.os }}-pip-\n```\n\n### Correct Usage\n\nThe following workflow checking out untrusted files, but the cache is scoped to the Pull Request.\n\n```yaml\nname: Secure Workflow\non:\n  pull_request:\n\njobs:\n  pr-comment:\n    permissions: read-all\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n      - name: Set up Python 3.10\n        uses: actions/setup-python@v5\n      - name: Cache pip dependencies\n        uses: actions/cache@v4\n        id: cache-pip\n        with:\n          path: ~/.cache/pip\n          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}\n          restore-keys: ${{ runner.os }}-pip-\n```\n\nNote, that the example above doesn't allow using secrets if the Pull Request originates from a fork. In case secrets are needed, `pull_request_target` with labels as `safe to test` can be used, but the code in Pull Request must be manually reviewed before applying the label.\n\n```yaml\nname: Secure Workflow\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  pr-comment:\n    if: contains(github.event.pull_request.labels.*.name, 'safe to test')\n    permissions: read-all\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n        with:\n          ref: ${{ github.event.pull_request.head.sha}}\n      - name: Set up Python 3.10\n        uses: actions/setup-python@v5\n      - name: Cache pip dependencies\n        uses: actions/cache@v4\n        id: cache-pip\n        with:\n          path: ~/.cache/pip\n          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}\n          restore-keys: ${{ runner.os }}-pip-\n```\n\n## References\n\n- Adnan Khan's Blog: [The Monsters in Your Build Cache – GitHub Actions Cache Poisoning](https://adnanthekhan.com/2024/05/06/the-monsters-in-your-build-cache-github-actions-cache-poisoning/).\n- GitHub Docs: [GitHub Actions Caching Documentation](https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows).\n- Scribe Security Blog: [Cache Poisoning in GitHub Actions](https://scribesecurity.com/blog/github-cache-poisoning/).\n","text":"## Overview\n\nGitHub Actions cache poisoning is a technique that allows an attacker to inject malicious content into the Action's cache from unprivileged workflow, potentially leading to code execution in privileged workflows.\n\nAn attacker with the ability to run code in the context of the default branch (e.g. through Code Injection or Execution of Untrusted Code) can exploit this to:\n\n1. Steal the cache access token and URL.\n2. Overflow the cache to trigger eviction of legitimate entries.\n3. Poison cache entries with malicious payloads.\n4. Achieve code execution in privileged workflows that restore the poisoned cache.\n\nThis allows lateral movement from low-privileged to high-privileged workflows within a repository.\n\n### Cache Structure\n\nIn GitHub Actions, cache scopes are primarily determined by the branch structure. Branches are considered the main security boundary for GitHub Actions caching. This means that cache entries are generally scoped to specific branches.\n\n- **Access to Parent Branch Caches**: Feature branches (or child branches) created off of a parent branch (like `main` or `dev`) can access caches from the parent branch. For instance, a feature branch off of `main` will be able to access the cache from `main`.\n\n- **Sibling Branches**: Sibling branches, meaning branches that are created from the same parent but not from each other, do not share caches. For example, two branches created off of `main` will not be able to access each other’s caches directly.\n\nDue to the above design, if something is cached in the context of the default branch (e.g., `main`), it becomes accessible to any feature branch derived from `main`.\n\n## Recommendation\n\n1. Avoid using caching in workflows that handle sensitive operations like releases.\n2. If caching must be used:\n   - Validate restored cache contents before use.\n   - Use short-lived, workflow-specific cache keys.\n   - Clear caches regularly.\n3. Implement strict isolation between untrusted and privileged workflow execution.\n4. Never run untrusted code in the context of the default branch.\n5. Sign the cache value cryptographically and verify the signature before usage.\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow is caching an attacker-controlled file (`large_file`) in the context of the default branch.\n\n```yaml\nname: Vulnerable Workflow\non:\n  issue_comment:\n    types: [created]\n\njobs:\n  pr-comment:\n    permissions: read-all\n    runs-on: ubuntu-latest\n    steps:\n      - uses: xt0rted/pull-request-comment-branch@v2\n        id: comment-branch\n      - uses: actions/checkout@v3\n        with:\n          ref: ${{ steps.comment-branch.outputs.head_sha }}\n      - name: Set up Python 3.10\n        uses: actions/setup-python@v5\n      - name: Cache pip dependencies\n        uses: actions/cache@v4\n        id: cache-pip\n        with:\n          path: ~/.cache/pip\n          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}\n          restore-keys: ${{ runner.os }}-pip-\n```\n\n### Correct Usage\n\nThe following workflow checking out untrusted files, but the cache is scoped to the Pull Request.\n\n```yaml\nname: Secure Workflow\non:\n  pull_request:\n\njobs:\n  pr-comment:\n    permissions: read-all\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n      - name: Set up Python 3.10\n        uses: actions/setup-python@v5\n      - name: Cache pip dependencies\n        uses: actions/cache@v4\n        id: cache-pip\n        with:\n          path: ~/.cache/pip\n          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}\n          restore-keys: ${{ runner.os }}-pip-\n```\n\nNote, that the example above doesn't allow using secrets if the Pull Request originates from a fork. In case secrets are needed, `pull_request_target` with labels as `safe to test` can be used, but the code in Pull Request must be manually reviewed before applying the label.\n\n```yaml\nname: Secure Workflow\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  pr-comment:\n    if: contains(github.event.pull_request.labels.*.name, 'safe to test')\n    permissions: read-all\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n        with:\n          ref: ${{ github.event.pull_request.head.sha}}\n      - name: Set up Python 3.10\n        uses: actions/setup-python@v5\n      - name: Cache pip dependencies\n        uses: actions/cache@v4\n        id: cache-pip\n        with:\n          path: ~/.cache/pip\n          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}\n          restore-keys: ${{ runner.os }}-pip-\n```\n\n## References\n\n- Adnan Khan's Blog: [The Monsters in Your Build Cache – GitHub Actions Cache Poisoning](https://adnanthekhan.com/2024/05/06/the-monsters-in-your-build-cache-github-actions-cache-poisoning/).\n- GitHub Docs: [GitHub Actions Caching Documentation](https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows).\n- Scribe Security Blog: [Cache Poisoning in GitHub Actions](https://scribesecurity.com/blog/github-cache-poisoning/).\n"},"id":"actions/cache-poisoning/direct-cache","name":"actions/cache-poisoning/direct-cache","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-349/CachePoisoningViaDirectCache.ql","security-severity":"7.5","tags":["actions","external/cwe/cwe-349","security"]},"shortDescription":{"text":"Cache Poisoning via caching of untrusted files"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"The cache can be poisoned by untrusted code, leading to a cache poisoning attack."},"help":{"markdown":"## Overview\n\nGitHub Actions cache poisoning is a technique that allows an attacker to inject malicious content into the Action's cache from unprivileged workflow, potentially leading to code execution in privileged workflows.\n\nAn attacker with the ability to run code in the context of the default branch (e.g. through Code Injection or Execution of Untrusted Code) can exploit this to:\n\n1. Steal the cache access token and URL.\n2. Overflow the cache to trigger eviction of legitimate entries.\n3. Poison cache entries with malicious payloads.\n4. Achieve code execution in privileged workflows that restore the poisoned cache.\n\nThis allows lateral movement from low-privileged to high-privileged workflows within a repository.\n\n### Cache Structure\n\nIn GitHub Actions, cache scopes are primarily determined by the branch structure. Branches are considered the main security boundary for GitHub Actions caching. This means that cache entries are generally scoped to specific branches.\n\n- **Access to Parent Branch Caches**: Feature branches (or child branches) created off of a parent branch (like `main` or `dev`) can access caches from the parent branch. For instance, a feature branch off of `main` will be able to access the cache from `main`.\n\n- **Sibling Branches**: Sibling branches, meaning branches that are created from the same parent but not from each other, do not share caches. For example, two branches created off of `main` will not be able to access each other’s caches directly.\n\nDue to the above design, if something is cached in the context of the default branch (e.g., `main`), it becomes accessible to any feature branch derived from `main`.\n\n## Recommendation\n\n1. Avoid using caching in workflows that handle sensitive operations like releases.\n2. If caching must be used:\n   - Validate restored cache contents before use.\n   - Use short-lived, workflow-specific cache keys.\n   - Clear caches regularly.\n3. Implement strict isolation between untrusted and privileged workflow execution.\n4. Never run untrusted code in the context of the default branch.\n5. Sign the cache value cryptographically and verify the signature before usage.\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow runs untrusted code in a non-privileged job but in the context of the default branch.\n\n```yaml\nname: Vulnerable Workflow\non:\n  pull_request_target:\n    branches: [main]\npermissions: {}\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n      - name: Run tests\n        run: ./run_tests.sh\n```\n\n### Correct Usage\n\nThe following workflow runs untrusted code in a non-privileged job and the cache is scoped to the Pull Request branch.\n\n```yaml\nname: Secure Workflow\non:\n  pull_request:\n    branches: [main]\npermissions: {}\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n      - name: Run tests\n        run: ./run_tests.sh\n```\n\n## References\n\n- Adnan Khan's Blog: [The Monsters in Your Build Cache – GitHub Actions Cache Poisoning](https://adnanthekhan.com/2024/05/06/the-monsters-in-your-build-cache-github-actions-cache-poisoning/).\n- GitHub Docs: [GitHub Actions Caching Documentation](https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows).\n- Scribe Security Blog: [Cache Poisoning in GitHub Actions](https://scribesecurity.com/blog/github-cache-poisoning/).\n","text":"## Overview\n\nGitHub Actions cache poisoning is a technique that allows an attacker to inject malicious content into the Action's cache from unprivileged workflow, potentially leading to code execution in privileged workflows.\n\nAn attacker with the ability to run code in the context of the default branch (e.g. through Code Injection or Execution of Untrusted Code) can exploit this to:\n\n1. Steal the cache access token and URL.\n2. Overflow the cache to trigger eviction of legitimate entries.\n3. Poison cache entries with malicious payloads.\n4. Achieve code execution in privileged workflows that restore the poisoned cache.\n\nThis allows lateral movement from low-privileged to high-privileged workflows within a repository.\n\n### Cache Structure\n\nIn GitHub Actions, cache scopes are primarily determined by the branch structure. Branches are considered the main security boundary for GitHub Actions caching. This means that cache entries are generally scoped to specific branches.\n\n- **Access to Parent Branch Caches**: Feature branches (or child branches) created off of a parent branch (like `main` or `dev`) can access caches from the parent branch. For instance, a feature branch off of `main` will be able to access the cache from `main`.\n\n- **Sibling Branches**: Sibling branches, meaning branches that are created from the same parent but not from each other, do not share caches. For example, two branches created off of `main` will not be able to access each other’s caches directly.\n\nDue to the above design, if something is cached in the context of the default branch (e.g., `main`), it becomes accessible to any feature branch derived from `main`.\n\n## Recommendation\n\n1. Avoid using caching in workflows that handle sensitive operations like releases.\n2. If caching must be used:\n   - Validate restored cache contents before use.\n   - Use short-lived, workflow-specific cache keys.\n   - Clear caches regularly.\n3. Implement strict isolation between untrusted and privileged workflow execution.\n4. Never run untrusted code in the context of the default branch.\n5. Sign the cache value cryptographically and verify the signature before usage.\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow runs untrusted code in a non-privileged job but in the context of the default branch.\n\n```yaml\nname: Vulnerable Workflow\non:\n  pull_request_target:\n    branches: [main]\npermissions: {}\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n      - name: Run tests\n        run: ./run_tests.sh\n```\n\n### Correct Usage\n\nThe following workflow runs untrusted code in a non-privileged job and the cache is scoped to the Pull Request branch.\n\n```yaml\nname: Secure Workflow\non:\n  pull_request:\n    branches: [main]\npermissions: {}\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n      - name: Run tests\n        run: ./run_tests.sh\n```\n\n## References\n\n- Adnan Khan's Blog: [The Monsters in Your Build Cache – GitHub Actions Cache Poisoning](https://adnanthekhan.com/2024/05/06/the-monsters-in-your-build-cache-github-actions-cache-poisoning/).\n- GitHub Docs: [GitHub Actions Caching Documentation](https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows).\n- Scribe Security Blog: [Cache Poisoning in GitHub Actions](https://scribesecurity.com/blog/github-cache-poisoning/).\n"},"id":"actions/cache-poisoning/poisonable-step","name":"actions/cache-poisoning/poisonable-step","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-349/CachePoisoningViaPoisonableStep.ql","security-severity":"7.5","tags":["actions","external/cwe/cwe-349","security"]},"shortDescription":{"text":"Cache Poisoning via execution of untrusted code"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"Interpreting unsanitized user input as code allows a malicious user to perform arbitrary code execution."},"help":{"markdown":"## Overview\n\nUsing user-controlled input in GitHub Actions may lead to code injection in contexts like _run:_ or _script:_.\n\nCode injection in GitHub Actions may allow an attacker to exfiltrate any secrets used in the workflow and the temporary GitHub repository authorization token. The token may have write access to the repository, allowing an attacker to make changes to the repository.\n\n## Recommendation\n\nThe best practice to avoid code injection vulnerabilities in GitHub workflows is to set the untrusted input value of the expression to an intermediate environment variable and then use the environment variable using the native syntax of the shell/script interpreter (that is, not _${{ env.VAR }}_).\n\nIt is also recommended to limit the permissions of any tokens used by a workflow such as the GITHUB_TOKEN.\n\n## Example\n\n### Incorrect Usage\n\nThe following example lets attackers inject an arbitrary shell command:\n\n```yaml\non: issue_comment\n\njobs:\n  echo-body:\n    runs-on: ubuntu-latest\n    steps:\n      - run: |\n          echo '${{ github.event.comment.body }}'\n```\n\nThe following example uses an environment variable, but **still allows the injection** because of the use of expression syntax:\n\n```yaml\non: issue_comment\n\njobs:\n  echo-body:\n    runs-on: ubuntu-latest\n    steps:\n    -  env:\n        BODY: ${{ github.event.issue.body }}\n      run: |\n        echo '${{ env.BODY }}'\n```\n\n### Correct Usage\n\nThe following example uses shell syntax to read the environment variable and will prevent the attack:\n\n```yaml\njobs:\n  echo-body:\n    runs-on: ubuntu-latest\n    steps:\n      - env:\n          BODY: ${{ github.event.issue.body }}\n        run: |\n          echo \"$BODY\"\n```\n\nThe following example uses `process.env` to read environment variables within JavaScript code.\n\n```yaml\njobs:\n  echo-body:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: uses: actions/github-script@v4\n        env:\n          BODY: ${{ github.event.issue.body }}\n        with:\n          script: |\n            const { BODY } = process.env\n            ...\n```\n\n## References\n\n- GitHub Security Lab Research: [Keeping your GitHub Actions and workflows secure: Untrusted input](https://securitylab.github.com/research/github-actions-untrusted-input).\n- GitHub Docs: [Security hardening for GitHub Actions](https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions).\n- GitHub Docs: [Permissions for the GITHUB_TOKEN](https://docs.github.com/en/actions/security-guides/automatic-token-authentication#permissions-for-the-github_token).\n","text":"## Overview\n\nUsing user-controlled input in GitHub Actions may lead to code injection in contexts like _run:_ or _script:_.\n\nCode injection in GitHub Actions may allow an attacker to exfiltrate any secrets used in the workflow and the temporary GitHub repository authorization token. The token may have write access to the repository, allowing an attacker to make changes to the repository.\n\n## Recommendation\n\nThe best practice to avoid code injection vulnerabilities in GitHub workflows is to set the untrusted input value of the expression to an intermediate environment variable and then use the environment variable using the native syntax of the shell/script interpreter (that is, not _${{ env.VAR }}_).\n\nIt is also recommended to limit the permissions of any tokens used by a workflow such as the GITHUB_TOKEN.\n\n## Example\n\n### Incorrect Usage\n\nThe following example lets attackers inject an arbitrary shell command:\n\n```yaml\non: issue_comment\n\njobs:\n  echo-body:\n    runs-on: ubuntu-latest\n    steps:\n      - run: |\n          echo '${{ github.event.comment.body }}'\n```\n\nThe following example uses an environment variable, but **still allows the injection** because of the use of expression syntax:\n\n```yaml\non: issue_comment\n\njobs:\n  echo-body:\n    runs-on: ubuntu-latest\n    steps:\n    -  env:\n        BODY: ${{ github.event.issue.body }}\n      run: |\n        echo '${{ env.BODY }}'\n```\n\n### Correct Usage\n\nThe following example uses shell syntax to read the environment variable and will prevent the attack:\n\n```yaml\njobs:\n  echo-body:\n    runs-on: ubuntu-latest\n    steps:\n      - env:\n          BODY: ${{ github.event.issue.body }}\n        run: |\n          echo \"$BODY\"\n```\n\nThe following example uses `process.env` to read environment variables within JavaScript code.\n\n```yaml\njobs:\n  echo-body:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: uses: actions/github-script@v4\n        env:\n          BODY: ${{ github.event.issue.body }}\n        with:\n          script: |\n            const { BODY } = process.env\n            ...\n```\n\n## References\n\n- GitHub Security Lab Research: [Keeping your GitHub Actions and workflows secure: Untrusted input](https://securitylab.github.com/research/github-actions-untrusted-input).\n- GitHub Docs: [Security hardening for GitHub Actions](https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions).\n- GitHub Docs: [Permissions for the GITHUB_TOKEN](https://docs.github.com/en/actions/security-guides/automatic-token-authentication#permissions-for-the-github_token).\n"},"id":"actions/code-injection/critical","name":"actions/code-injection/critical","properties":{"precision":"very-high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-094/CodeInjectionCritical.ql","security-severity":"9","tags":["actions","external/cwe/cwe-094","external/cwe/cwe-095","external/cwe/cwe-116","security"]},"shortDescription":{"text":"Code injection"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"Building the PATH environment variable from user-controlled sources may alter the execution of following system commands"},"help":{"markdown":"## Overview\n\nGitHub Actions allow to define the system PATH variable by writing to a file pointed to by the `GITHUB_PATH` environment variable. Writing to this file appends a directory to the system PATH variable and automatically makes it available to all subsequent actions in the current job.\n\nE.g.:\n\n```bash\necho \"$HOME/.local/bin\" \u003e\u003e $GITHUB_PATH\n```\n\nIf an attacker can control the contents of the system PATH, they are able to influence what commands are run in subsequent steps of the same job.\n\n## Recommendation\n\nDo not allow untrusted data to influence the system PATH: Avoid using untrusted data sources (e.g., artifact content) to define the system PATH.\n\n## Example\n\n### Incorrect Usage\n\nConsider the following basic setup where an environment variable `PATH` is set:\n\n```yaml\nsteps:\n  - name: Set the path\n    env:\n      BODY: ${{ github.event.comment.body }}\n    run: |\n      PATH=$(echo \"$BODY\" | grep -oP 'system path: \\K\\S+')\n      echo \"$PATH\" \u003e\u003e \"$GITHUB_PATH\"\n```\n\nIf an attacker can manipulate the value being set, such as through artifact downloads or user inputs, they can potentially change the system PATH and get arbitrary command execution in subsequent steps.\n\n## References\n\n- GitHub Docs: [Workflow commands for GitHub Actions](https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/workflow-commands-for-github-actions).\n","text":"## Overview\n\nGitHub Actions allow to define the system PATH variable by writing to a file pointed to by the `GITHUB_PATH` environment variable. Writing to this file appends a directory to the system PATH variable and automatically makes it available to all subsequent actions in the current job.\n\nE.g.:\n\n```bash\necho \"$HOME/.local/bin\" \u003e\u003e $GITHUB_PATH\n```\n\nIf an attacker can control the contents of the system PATH, they are able to influence what commands are run in subsequent steps of the same job.\n\n## Recommendation\n\nDo not allow untrusted data to influence the system PATH: Avoid using untrusted data sources (e.g., artifact content) to define the system PATH.\n\n## Example\n\n### Incorrect Usage\n\nConsider the following basic setup where an environment variable `PATH` is set:\n\n```yaml\nsteps:\n  - name: Set the path\n    env:\n      BODY: ${{ github.event.comment.body }}\n    run: |\n      PATH=$(echo \"$BODY\" | grep -oP 'system path: \\K\\S+')\n      echo \"$PATH\" \u003e\u003e \"$GITHUB_PATH\"\n```\n\nIf an attacker can manipulate the value being set, such as through artifact downloads or user inputs, they can potentially change the system PATH and get arbitrary command execution in subsequent steps.\n\n## References\n\n- GitHub Docs: [Workflow commands for GitHub Actions](https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/workflow-commands-for-github-actions).\n"},"id":"actions/envpath-injection/critical","name":"actions/envpath-injection/critical","properties":{"precision":"very-high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-077/EnvPathInjectionCritical.ql","security-severity":"9","tags":["actions","external/cwe/cwe-020","external/cwe/cwe-077","security"]},"shortDescription":{"text":"PATH environment variable built from user-controlled sources"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"Building an environment variable from user-controlled sources may alter the execution of following system commands"},"help":{"markdown":"## Overview\n\nGitHub Actions allow to define environment variables by writing to a file pointed to by the `GITHUB_ENV` environment variable:\n\nThis file contains lines in the `KEY=VALUE` format:\n\n```bash\nsteps:\n  - name: Set the value\n    id: step_one\n    run: |\n      echo \"action_state=yellow\" \u003e\u003e \"$GITHUB_ENV\"\n```\n\nIt is also possible to define multiline variables by using the [following construct](https://en.wikipedia.org/wiki/Here_document):\n\n```\nKEY\u003c\u003c{delimiter}\nVALUE\nVALUE\n{delimiter}\n```\n\n```bash\nsteps:\n  - name: Set the value in bash\n    id: step_one\n    run: |\n      {\n        echo 'JSON_RESPONSE\u003c\u003cEOF'\n        curl https://example.com\n        echo EOF\n      } \u003e\u003e \"$GITHUB_ENV\"\n```\n\nIf an attacker can control the values assigned to environment variables and there is no sanitization in place, the attacker will be able to inject additional variables by injecting new lines or `{delimiters}`.\n\n## Recommendation\n\n1. **Do not allow untrusted data to influence environment variables**:\n\n    - Avoid using untrusted data sources (e.g., artifact content) to define environment variables.\n    - Validate and sanitize all inputs before using them in environment settings.\n\n2. **Do not allow new lines when defining single line environment variables**:\n\n    - `echo \"BODY=$(echo \"$BODY\" | tr -d '\\n')\" \u003e\u003e \"$GITHUB_ENV\"`\n\n3. **Use unique identifiers when defining multi line environment variables**:\n\n    ```bash\n    steps:\n      - name: Set the value in bash\n        id: step_one\n        run: |\n          # Generate a UUID\n          UUID=$(uuidgen)\n          {\n            echo \"JSON_RESPONSE\u003c\u003cEOF$UUID\"\n            curl https://example.com\n            echo \"EOF$UUID\"\n          } \u003e\u003e \"$GITHUB_ENV\"\n    ```\n\n## Example\n\n### Example of Vulnerability\n\nConsider the following basic setup where an environment variable `MYVAR` is set and used in subsequent steps:\n\n```yaml\nsteps:\n  - name: Set the value\n    id: step_one\n    env:\n      BODY: ${{ github.event.comment.body }}\n    run: |\n      REPLACED=$(echo \"$BODY\" | sed 's/FOO/BAR/g')\n      echo \"MYVAR=$REPLACED\" \u003e\u003e \"$GITHUB_ENV\"\n```\n\nIf an attacker can manipulate the value being set, such as through artifact downloads or user inputs, the attacker can potentially inject new environment variables. For example, they could write an issue comment like:\n\n```text\nFOO\nNEW_ENV_VAR=MALICIOUS_VALUE\n```\n\nLikewise, if the attacker controls a file in the GitHub Actions Runner's workspace (eg: the workflow checkouts untrusted code or downloads an untrusted artifact) and the contents of that file are assigned to an environment variable such as:\n\n```bash\n- run: |\n    PR_NUMBER=$(cat pr-number.txt)\n    echo \"PR_NUMBER=$PR_NUMBER\" \u003e\u003e $GITHUB_ENV\n```\n\nAn attacker could craft a malicious artifact that writes dangerous environment variables:\n\n```bash\n  - run: |\n      echo -e \"666\\nNEW_ENV_VAR=MALICIOUS_VALUE\" \u003e pr-number.txt\n  - uses: actions/upload-artifact@v4\n    with:\n      name: pr-number\n      path: ./pr-number.txt\n```\n\n### Exploitation\n\nAn attacker is be able to run arbitrary code by injecting environment variables such as `LD_PRELOAD`, `BASH_ENV`, etc.\n\n## References\n\n- GitHub Docs: [Workflow commands for GitHub Actions](https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/workflow-commands-for-github-actions).\n- Synacktiv: [GitHub Actions Exploitation: Repo Jacking and Environment Manipulation](https://www.synacktiv.com/publications/github-actions-exploitation-repo-jacking-and-environment-manipulation).\n","text":"## Overview\n\nGitHub Actions allow to define environment variables by writing to a file pointed to by the `GITHUB_ENV` environment variable:\n\nThis file contains lines in the `KEY=VALUE` format:\n\n```bash\nsteps:\n  - name: Set the value\n    id: step_one\n    run: |\n      echo \"action_state=yellow\" \u003e\u003e \"$GITHUB_ENV\"\n```\n\nIt is also possible to define multiline variables by using the [following construct](https://en.wikipedia.org/wiki/Here_document):\n\n```\nKEY\u003c\u003c{delimiter}\nVALUE\nVALUE\n{delimiter}\n```\n\n```bash\nsteps:\n  - name: Set the value in bash\n    id: step_one\n    run: |\n      {\n        echo 'JSON_RESPONSE\u003c\u003cEOF'\n        curl https://example.com\n        echo EOF\n      } \u003e\u003e \"$GITHUB_ENV\"\n```\n\nIf an attacker can control the values assigned to environment variables and there is no sanitization in place, the attacker will be able to inject additional variables by injecting new lines or `{delimiters}`.\n\n## Recommendation\n\n1. **Do not allow untrusted data to influence environment variables**:\n\n    - Avoid using untrusted data sources (e.g., artifact content) to define environment variables.\n    - Validate and sanitize all inputs before using them in environment settings.\n\n2. **Do not allow new lines when defining single line environment variables**:\n\n    - `echo \"BODY=$(echo \"$BODY\" | tr -d '\\n')\" \u003e\u003e \"$GITHUB_ENV\"`\n\n3. **Use unique identifiers when defining multi line environment variables**:\n\n    ```bash\n    steps:\n      - name: Set the value in bash\n        id: step_one\n        run: |\n          # Generate a UUID\n          UUID=$(uuidgen)\n          {\n            echo \"JSON_RESPONSE\u003c\u003cEOF$UUID\"\n            curl https://example.com\n            echo \"EOF$UUID\"\n          } \u003e\u003e \"$GITHUB_ENV\"\n    ```\n\n## Example\n\n### Example of Vulnerability\n\nConsider the following basic setup where an environment variable `MYVAR` is set and used in subsequent steps:\n\n```yaml\nsteps:\n  - name: Set the value\n    id: step_one\n    env:\n      BODY: ${{ github.event.comment.body }}\n    run: |\n      REPLACED=$(echo \"$BODY\" | sed 's/FOO/BAR/g')\n      echo \"MYVAR=$REPLACED\" \u003e\u003e \"$GITHUB_ENV\"\n```\n\nIf an attacker can manipulate the value being set, such as through artifact downloads or user inputs, the attacker can potentially inject new environment variables. For example, they could write an issue comment like:\n\n```text\nFOO\nNEW_ENV_VAR=MALICIOUS_VALUE\n```\n\nLikewise, if the attacker controls a file in the GitHub Actions Runner's workspace (eg: the workflow checkouts untrusted code or downloads an untrusted artifact) and the contents of that file are assigned to an environment variable such as:\n\n```bash\n- run: |\n    PR_NUMBER=$(cat pr-number.txt)\n    echo \"PR_NUMBER=$PR_NUMBER\" \u003e\u003e $GITHUB_ENV\n```\n\nAn attacker could craft a malicious artifact that writes dangerous environment variables:\n\n```bash\n  - run: |\n      echo -e \"666\\nNEW_ENV_VAR=MALICIOUS_VALUE\" \u003e pr-number.txt\n  - uses: actions/upload-artifact@v4\n    with:\n      name: pr-number\n      path: ./pr-number.txt\n```\n\n### Exploitation\n\nAn attacker is be able to run arbitrary code by injecting environment variables such as `LD_PRELOAD`, `BASH_ENV`, etc.\n\n## References\n\n- GitHub Docs: [Workflow commands for GitHub Actions](https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/workflow-commands-for-github-actions).\n- Synacktiv: [GitHub Actions Exploitation: Repo Jacking and Environment Manipulation](https://www.synacktiv.com/publications/github-actions-exploitation-repo-jacking-and-environment-manipulation).\n"},"id":"actions/envvar-injection/critical","name":"actions/envvar-injection/critical","properties":{"precision":"very-high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-077/EnvVarInjectionCritical.ql","security-severity":"9","tags":["actions","external/cwe/cwe-020","external/cwe/cwe-077","security"]},"shortDescription":{"text":"Environment variable built from user-controlled sources"}},{"defaultConfiguration":{"level":"warning"},"fullDescription":{"text":"All organization and repository secrets are passed to the workflow runner."},"help":{"markdown":"## Overview\n\nWhen the workflow runner cannot determine what secrets are needed to run the workflow, it will pass all the available secrets to the runner including organization and repository secrets. This violates the least privileged principle and increases the impact of a potential vulnerability affecting the workflow.\n\n## Recommendation\n\nOnly pass those secrets that are needed by the workflow. Avoid using expressions such as `toJSON(secrets)` or dynamically accessed secrets such as `secrets[format('GH_PAT_%s', matrix.env)]` since the workflow will need to receive all secrets to decide at runtime which one needs to be used.\n\n## Example\n\n### Incorrect Usage\n\n```yaml\nenv:\n  ALL_SECRETS: ${{ toJSON(secrets) }}\n```\n\n```yaml\nstrategy:\n  matrix:\n    env: [PROD, DEV]\nenv:\n  GH_TOKEN: ${{ secrets[format('GH_PAT_%s', matrix.env)] }}\n```\n\n### Correct Usage\n\n```yaml\nenv:\n  NEEDED_SECRET: ${{ secrets.GH_PAT }}\n```\n\n```yaml\nstrategy:\n  matrix:\n    env: [PROD, DEV]\n---\nif: matrix.env == \"PROD\"\nenv:\n  GH_TOKEN: ${{ secrets.GH_PAT_PROD }}\n---\nif: matrix.env == \"DEV\"\nenv:\n  GH_TOKEN: ${{ secrets.GH_PAT_DEV }}\n```\n\n## References\n\n- GitHub Docs: [Using secrets in GitHub Actions](https://docs.github.com/en/actions/security-for-github-actions/security-guides/using-secrets-in-github-actions#using-encrypted-secrets-in-a-workflow).\n- poutine: [Job uses all secrets](https://github.com/boostsecurityio/poutine/blob/main/docs/content/en/rules/job_all_secrets.md).\n","text":"## Overview\n\nWhen the workflow runner cannot determine what secrets are needed to run the workflow, it will pass all the available secrets to the runner including organization and repository secrets. This violates the least privileged principle and increases the impact of a potential vulnerability affecting the workflow.\n\n## Recommendation\n\nOnly pass those secrets that are needed by the workflow. Avoid using expressions such as `toJSON(secrets)` or dynamically accessed secrets such as `secrets[format('GH_PAT_%s', matrix.env)]` since the workflow will need to receive all secrets to decide at runtime which one needs to be used.\n\n## Example\n\n### Incorrect Usage\n\n```yaml\nenv:\n  ALL_SECRETS: ${{ toJSON(secrets) }}\n```\n\n```yaml\nstrategy:\n  matrix:\n    env: [PROD, DEV]\nenv:\n  GH_TOKEN: ${{ secrets[format('GH_PAT_%s', matrix.env)] }}\n```\n\n### Correct Usage\n\n```yaml\nenv:\n  NEEDED_SECRET: ${{ secrets.GH_PAT }}\n```\n\n```yaml\nstrategy:\n  matrix:\n    env: [PROD, DEV]\n---\nif: matrix.env == \"PROD\"\nenv:\n  GH_TOKEN: ${{ secrets.GH_PAT_PROD }}\n---\nif: matrix.env == \"DEV\"\nenv:\n  GH_TOKEN: ${{ secrets.GH_PAT_DEV }}\n```\n\n## References\n\n- GitHub Docs: [Using secrets in GitHub Actions](https://docs.github.com/en/actions/security-for-github-actions/security-guides/using-secrets-in-github-actions#using-encrypted-secrets-in-a-workflow).\n- poutine: [Job uses all secrets](https://github.com/boostsecurityio/poutine/blob/main/docs/content/en/rules/job_all_secrets.md).\n"},"id":"actions/excessive-secrets-exposure","name":"actions/excessive-secrets-exposure","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-312/ExcessiveSecretsExposure.ql","security-severity":"5","tags":["actions","external/cwe/cwe-312","security"]},"shortDescription":{"text":"Excessive Secrets Exposure"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"The access control mechanism is not properly implemented, allowing untrusted code to be executed in a privileged context."},"help":{"markdown":"## Overview\n\nSometimes labels are used to approve GitHub Actions. An authorization check may not be properly implemented, allowing an attacker to mutate the code after it has been reviewed and approved by label.\n\n## Recommendation\n\nWhen using labels, make sure that the code cannot be modified after it has been reviewed and the label has been set.\n\n## Example\n\n### Incorrect Usage\n\nThe following example shows a job that requires the label `safe to test` to be set before running untrusted code. There are two problems with the code:\n\n1. The workflow gets triggered on `synchronize` activity type and, therefore, it will get triggered every time there is a change in the Pull Request. An attacker can modify the code of the Pull Request after the code has been reviewed and the label has been set. The workflow will be triggered every time a new change is added to the Pull Request.\n2. The workflow uses `ref: ${{ github.event.pull_request.head.ref }}` for checkout, which is a branch name of the Pull Request. There is a window of opportunity for the attacker to modify their branch after the Pull Request is labeled, but before the workflow starts and runs the checkout.\n\n```yaml\non:\n  pull_request_target:\n    types: [opened, synchronize]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout repo for OWNER TEST\n        uses: actions/checkout@v3\n        if: contains(github.event.pull_request.labels.*.name, 'safe to test')\n        with:\n          ref: ${{ github.event.pull_request.head.ref }}\n      - run: ./cmd\n```\n\n### Correct Usage\n\nMake sure that the workflow only gets triggered when the label is set and use an immutable commit (`github.event.pull_request.head.sha`) instead of a mutable reference.\n\n```yaml\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout repo for OWNER TEST\n        uses: actions/checkout@v3\n        if: contains(github.event.pull_request.labels.*.name, 'safe to test')\n        with:\n          ref: ${{ github.event.pull_request.head.sha}}\n      - run: ./cmd\n```\n\n## References\n\n- GitHub Docs: [Events that trigger workflows](https://docs.github.com/en/actions/writing-workflows/choosing-when-your-workflow-runs/events-that-trigger-workflows#pull_request_target).\n","text":"## Overview\n\nSometimes labels are used to approve GitHub Actions. An authorization check may not be properly implemented, allowing an attacker to mutate the code after it has been reviewed and approved by label.\n\n## Recommendation\n\nWhen using labels, make sure that the code cannot be modified after it has been reviewed and the label has been set.\n\n## Example\n\n### Incorrect Usage\n\nThe following example shows a job that requires the label `safe to test` to be set before running untrusted code. There are two problems with the code:\n\n1. The workflow gets triggered on `synchronize` activity type and, therefore, it will get triggered every time there is a change in the Pull Request. An attacker can modify the code of the Pull Request after the code has been reviewed and the label has been set. The workflow will be triggered every time a new change is added to the Pull Request.\n2. The workflow uses `ref: ${{ github.event.pull_request.head.ref }}` for checkout, which is a branch name of the Pull Request. There is a window of opportunity for the attacker to modify their branch after the Pull Request is labeled, but before the workflow starts and runs the checkout.\n\n```yaml\non:\n  pull_request_target:\n    types: [opened, synchronize]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout repo for OWNER TEST\n        uses: actions/checkout@v3\n        if: contains(github.event.pull_request.labels.*.name, 'safe to test')\n        with:\n          ref: ${{ github.event.pull_request.head.ref }}\n      - run: ./cmd\n```\n\n### Correct Usage\n\nMake sure that the workflow only gets triggered when the label is set and use an immutable commit (`github.event.pull_request.head.sha`) instead of a mutable reference.\n\n```yaml\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout repo for OWNER TEST\n        uses: actions/checkout@v3\n        if: contains(github.event.pull_request.labels.*.name, 'safe to test')\n        with:\n          ref: ${{ github.event.pull_request.head.sha}}\n      - run: ./cmd\n```\n\n## References\n\n- GitHub Docs: [Events that trigger workflows](https://docs.github.com/en/actions/writing-workflows/choosing-when-your-workflow-runs/events-that-trigger-workflows#pull_request_target).\n"},"id":"actions/improper-access-control","name":"actions/improper-access-control","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-285/ImproperAccessControl.ql","security-severity":"9.3","tags":["actions","external/cwe/cwe-285","security"]},"shortDescription":{"text":"Improper Access Control"}},{"defaultConfiguration":{"level":"warning"},"fullDescription":{"text":"Workflows should contain explicit permissions to restrict the scope of the default GITHUB_TOKEN."},"help":{"markdown":"## Overview\n\nIf a GitHub Actions job or workflow has no explicit permissions set, then the repository permissions are used. Repositories created under organizations inherit the organization permissions. The organizations or repositories created before February 2023 have the default permissions set to read-write. Often these permissions do not adhere to the principle of least privilege and can be reduced to read-only, leaving the `write` permission only to a specific types as `issues: write` or `pull-requests: write`.\n\nNote that this query cannot check whether the organization or repository token settings are set to read-only. However, even if they are, it is recommended to define explicit permissions (`contents: read` and `packages: read` are equivalent to the read-only default) so that (a) the actual needs of the workflow are documented, and (b) the permissions will remain restricted if the default is subsequently changed, or the workflow is copied to a different repository or organization.\n\n## Recommendation\n\nAdd the `permissions` key to the job or the root of workflow (in this case it is applied to all jobs in the workflow that do not have their own `permissions` key) and assign the least privileges required to complete the task.\n\n## Example\n\n### Incorrect Usage\n\n```yaml\nname: \"My workflow\"\n# No permissions block\n```\n\n### Correct Usage\n\n```yaml\nname: \"My workflow\"\npermissions:\n  contents: read\n  pull-requests: write\n```\n\nor\n\n```yaml\njobs:\n  my-job:\n    permissions:\n      contents: read\n      pull-requests: write\n```\n\n## References\n\n- GitHub Docs: [Assigning permissions to jobs](https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/assigning-permissions-to-jobs).\n","text":"## Overview\n\nIf a GitHub Actions job or workflow has no explicit permissions set, then the repository permissions are used. Repositories created under organizations inherit the organization permissions. The organizations or repositories created before February 2023 have the default permissions set to read-write. Often these permissions do not adhere to the principle of least privilege and can be reduced to read-only, leaving the `write` permission only to a specific types as `issues: write` or `pull-requests: write`.\n\nNote that this query cannot check whether the organization or repository token settings are set to read-only. However, even if they are, it is recommended to define explicit permissions (`contents: read` and `packages: read` are equivalent to the read-only default) so that (a) the actual needs of the workflow are documented, and (b) the permissions will remain restricted if the default is subsequently changed, or the workflow is copied to a different repository or organization.\n\n## Recommendation\n\nAdd the `permissions` key to the job or the root of workflow (in this case it is applied to all jobs in the workflow that do not have their own `permissions` key) and assign the least privileges required to complete the task.\n\n## Example\n\n### Incorrect Usage\n\n```yaml\nname: \"My workflow\"\n# No permissions block\n```\n\n### Correct Usage\n\n```yaml\nname: \"My workflow\"\npermissions:\n  contents: read\n  pull-requests: write\n```\n\nor\n\n```yaml\njobs:\n  my-job:\n    permissions:\n      contents: read\n      pull-requests: write\n```\n\n## References\n\n- GitHub Docs: [Assigning permissions to jobs](https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/assigning-permissions-to-jobs).\n"},"id":"actions/missing-workflow-permissions","name":"actions/missing-workflow-permissions","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-275/MissingActionsPermissions.ql","security-severity":"5","tags":["actions","external/cwe/cwe-275","maintainability","security"]},"shortDescription":{"text":"Workflow does not contain permissions"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"Including sensitive information in a GitHub Actions artifact can expose it to an attacker."},"help":{"markdown":"## Overview\n\nSensitive information included in a GitHub Actions artifact can allow an attacker to access the sensitive information if the artifact is published.\n\n## Recommendation\n\nOnly store information that is meant to be publicly available in a GitHub Actions artifact.\n\n## Example\n\n### Incorrect Usage\n\nThe following example uses `actions/checkout` to checkout code which stores the GITHUB_TOKEN in the \\`.git/config\\` file and then stores the contents of the \\`.git\\` repository into the artifact:\n\n```yaml\nname: secrets-in-artifacts\non:\n  pull_request:\njobs:\n  a-job: # VULNERABLE\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: \"Upload artifact\"\n        uses: actions/upload-artifact@1746f4ab65b179e0ea60a494b83293b640dd5bba # v4.3.2\n        with:\n          name: file\n          path: .\n```\n\n### Correct Usage\n\nThe issue has been fixed below, where the `actions/upload-artifact` uses a version (v4+) which does not include hidden files or directories into the artifact.\n\n```yaml\nname: secrets-in-artifacts\non:\n  pull_request:\njobs:\n  a-job: # NOT VULNERABLE\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: \"Upload artifact\"\n        uses: actions/upload-artifact@v4\n        with:\n          name: file\n          path: .\n```\n","text":"## Overview\n\nSensitive information included in a GitHub Actions artifact can allow an attacker to access the sensitive information if the artifact is published.\n\n## Recommendation\n\nOnly store information that is meant to be publicly available in a GitHub Actions artifact.\n\n## Example\n\n### Incorrect Usage\n\nThe following example uses `actions/checkout` to checkout code which stores the GITHUB_TOKEN in the \\`.git/config\\` file and then stores the contents of the \\`.git\\` repository into the artifact:\n\n```yaml\nname: secrets-in-artifacts\non:\n  pull_request:\njobs:\n  a-job: # VULNERABLE\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: \"Upload artifact\"\n        uses: actions/upload-artifact@1746f4ab65b179e0ea60a494b83293b640dd5bba # v4.3.2\n        with:\n          name: file\n          path: .\n```\n\n### Correct Usage\n\nThe issue has been fixed below, where the `actions/upload-artifact` uses a version (v4+) which does not include hidden files or directories into the artifact.\n\n```yaml\nname: secrets-in-artifacts\non:\n  pull_request:\njobs:\n  a-job: # NOT VULNERABLE\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: \"Upload artifact\"\n        uses: actions/upload-artifact@v4\n        with:\n          name: file\n          path: .\n```\n"},"id":"actions/secrets-in-artifacts","name":"actions/secrets-in-artifacts","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-312/SecretsInArtifacts.ql","security-severity":"7.5","tags":["actions","external/cwe/cwe-312","security"]},"shortDescription":{"text":"Storage of sensitive information in GitHub Actions artifact"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"Secrets derived from other secrets are not masked by the workflow runner."},"help":{"markdown":"## Overview\n\nSecrets derived from other secrets are not known to the workflow runner, and therefore are not masked unless explicitly registered.\n\n## Recommendation\n\nAvoid defining non-plain secrets. For example, do not define a new secret containing a JSON object and then read properties out of it from the workflow, since these read values will not be masked by the workflow runner.\n\n## Example\n\n### Incorrect Usage\n\n```yaml\n- env:\n    username: ${{ fromJson(secrets.AZURE_CREDENTIALS).clientId }}\n    password: ${{ fromJson(secrets.AZURE_CREDENTIALS).clientSecret }}\n  run: |\n    echo \"$username\"\n    echo \"$password\"\n```\n\n### Correct Usage\n\n```yaml\n- env:\n    username: ${{ secrets.AZURE_CREDENTIALS_CLIENT_ID }}\n    password: ${{ secrets.AZURE_CREDENTIALS_CLIENT_SECRET }}\n  run: |\n    echo \"$username\"\n    echo \"$password\"\n```\n\n## References\n\n- GitHub Docs: [Using secrets in GitHub Actions](https://docs.github.com/en/actions/security-for-github-actions/security-guides/using-secrets-in-github-actions#using-encrypted-secrets-in-a-workflow).\n","text":"## Overview\n\nSecrets derived from other secrets are not known to the workflow runner, and therefore are not masked unless explicitly registered.\n\n## Recommendation\n\nAvoid defining non-plain secrets. For example, do not define a new secret containing a JSON object and then read properties out of it from the workflow, since these read values will not be masked by the workflow runner.\n\n## Example\n\n### Incorrect Usage\n\n```yaml\n- env:\n    username: ${{ fromJson(secrets.AZURE_CREDENTIALS).clientId }}\n    password: ${{ fromJson(secrets.AZURE_CREDENTIALS).clientSecret }}\n  run: |\n    echo \"$username\"\n    echo \"$password\"\n```\n\n### Correct Usage\n\n```yaml\n- env:\n    username: ${{ secrets.AZURE_CREDENTIALS_CLIENT_ID }}\n    password: ${{ secrets.AZURE_CREDENTIALS_CLIENT_SECRET }}\n  run: |\n    echo \"$username\"\n    echo \"$password\"\n```\n\n## References\n\n- GitHub Docs: [Using secrets in GitHub Actions](https://docs.github.com/en/actions/security-for-github-actions/security-guides/using-secrets-in-github-actions#using-encrypted-secrets-in-a-workflow).\n"},"id":"actions/unmasked-secret-exposure","name":"actions/unmasked-secret-exposure","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-312/UnmaskedSecretExposure.ql","security-severity":"9","tags":["actions","external/cwe/cwe-312","security"]},"shortDescription":{"text":"Unmasked Secret Exposure"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"Untrusted Checkout is protected by a security check but the checked-out branch can be changed after the check."},"help":{"markdown":"## Overview\n\nUntrusted Checkout is protected by a security check but the checked-out branch can be changed after the check.\n\n## Recommendation\n\nVerify that the code has not been modified after the security check. This may be achieved differently depending on the type of check:\n\n- Deployment Environment Approval: Make sure to use a non-mutable reference to the code to be executed. For example use a `sha` instead of a `ref`.\n- Label Gates: Make sure to use a non-mutable reference to the code to be executed. For example use a `sha` instead of a `ref`.\n\n## Example\n\n### Incorrect Usage (Deployment Environment Approval)\n\nThe following workflow uses a Deployment Environment which may be configured to require an approval. However, it check outs the code pointed to by the Pull Request branch reference. At attacker could submit legitimate code for review and then change it once it gets approved.\n\n```yml\non:\n  pull_request_target:\n    types: [Created]\njobs:\n  test:\n    environment: NeedsApproval\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout from PR branch\n        uses: actions/checkout@v4\n        with:\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n          ref: ${{ github.event.pull_request.head.ref }}\n      - run: ./cmd\n```\n\n### Correct Usage (Deployment Environment Approval)\n\nUse immutable references (Commit SHA) to make sure that the reviewed code does not change between the check and the use.\n\n```yml\non:\n  pull_request_target:\n    types: [Created]\njobs:\n  test:\n    environment: NeedsApproval\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout from PR branch\n        uses: actions/checkout@v4\n        with:\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n          ref: ${{ github.event.pull_request.head.sha }}\n      - run: ./cmd\n```\n\n### Incorrect Usage (Label Gates)\n\nThe following workflow uses a Deployment Environment which may be configured to require an approval. However, it check outs the code pointed to by the Pull Request branch reference. At attacker could submit legitimate code for review and then change it once it gets approved.\n\n```yaml\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    if: contains(github.event.pull_request.labels.*.name, 'safe-to-test')\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          ref: ${{ github.event.pull_request.head.ref }}\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n      - run: ./cmd\n```\n\n### Correct Usage (Label Gates)\n\nUse immutable references (Commit SHA) to make sure that the reviewed code does not change between the check and the use.\n\n```yaml\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    if: contains(github.event.pull_request.labels.*.name, 'safe-to-test')\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n      - run: ./cmd\n```\n\n## References\n\n- [ActionsTOCTOU](https://github.com/AdnaneKhan/ActionsTOCTOU).\n","text":"## Overview\n\nUntrusted Checkout is protected by a security check but the checked-out branch can be changed after the check.\n\n## Recommendation\n\nVerify that the code has not been modified after the security check. This may be achieved differently depending on the type of check:\n\n- Deployment Environment Approval: Make sure to use a non-mutable reference to the code to be executed. For example use a `sha` instead of a `ref`.\n- Label Gates: Make sure to use a non-mutable reference to the code to be executed. For example use a `sha` instead of a `ref`.\n\n## Example\n\n### Incorrect Usage (Deployment Environment Approval)\n\nThe following workflow uses a Deployment Environment which may be configured to require an approval. However, it check outs the code pointed to by the Pull Request branch reference. At attacker could submit legitimate code for review and then change it once it gets approved.\n\n```yml\non:\n  pull_request_target:\n    types: [Created]\njobs:\n  test:\n    environment: NeedsApproval\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout from PR branch\n        uses: actions/checkout@v4\n        with:\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n          ref: ${{ github.event.pull_request.head.ref }}\n      - run: ./cmd\n```\n\n### Correct Usage (Deployment Environment Approval)\n\nUse immutable references (Commit SHA) to make sure that the reviewed code does not change between the check and the use.\n\n```yml\non:\n  pull_request_target:\n    types: [Created]\njobs:\n  test:\n    environment: NeedsApproval\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout from PR branch\n        uses: actions/checkout@v4\n        with:\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n          ref: ${{ github.event.pull_request.head.sha }}\n      - run: ./cmd\n```\n\n### Incorrect Usage (Label Gates)\n\nThe following workflow uses a Deployment Environment which may be configured to require an approval. However, it check outs the code pointed to by the Pull Request branch reference. At attacker could submit legitimate code for review and then change it once it gets approved.\n\n```yaml\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    if: contains(github.event.pull_request.labels.*.name, 'safe-to-test')\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          ref: ${{ github.event.pull_request.head.ref }}\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n      - run: ./cmd\n```\n\n### Correct Usage (Label Gates)\n\nUse immutable references (Commit SHA) to make sure that the reviewed code does not change between the check and the use.\n\n```yaml\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    if: contains(github.event.pull_request.labels.*.name, 'safe-to-test')\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n      - run: ./cmd\n```\n\n## References\n\n- [ActionsTOCTOU](https://github.com/AdnaneKhan/ActionsTOCTOU).\n"},"id":"actions/untrusted-checkout-toctou/critical","name":"actions/untrusted-checkout-toctou/critical","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-367/UntrustedCheckoutTOCTOUCritical.ql","security-severity":"9.3","tags":["actions","external/cwe/cwe-367","security"]},"shortDescription":{"text":"Untrusted Checkout TOCTOU"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"Untrusted Checkout is protected by a security check but the checked-out branch can be changed after the check."},"help":{"markdown":"## Overview\n\nUntrusted Checkout is protected by a security check but the checked-out branch can be changed after the check.\n\n## Recommendation\n\nVerify that the code has not been modified after the security check. This may be achieved differently depending on the type of check:\n\n- Deployment Environment Approval: Make sure to use a non-mutable reference to the code to be executed. For example use a `sha` instead of a `ref`.\n- Label Gates: Make sure to use a non-mutable reference to the code to be executed. For example use a `sha` instead of a `ref`.\n\n## Example\n\n### Incorrect Usage (Deployment Environment Approval)\n\nThe following workflow uses a Deployment Environment which may be configured to require an approval. However, it check outs the code pointed to by the Pull Request branch reference. At attacker could submit legitimate code for review and then change it once it gets approved.\n\n```yml\non:\n  pull_request_target:\n    types: [Created]\njobs:\n  test:\n    environment: NeedsApproval\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout from PR branch\n        uses: actions/checkout@v4\n        with:\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n          ref: ${{ github.event.pull_request.head.ref }}\n      - run: ./cmd\n```\n\n### Correct Usage (Deployment Environment Approval)\n\nUse immutable references (Commit SHA) to make sure that the reviewed code does not change between the check and the use.\n\n```yml\non:\n  pull_request_target:\n    types: [Created]\njobs:\n  test:\n    environment: NeedsApproval\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout from PR branch\n        uses: actions/checkout@v4\n        with:\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n          ref: ${{ github.event.pull_request.head.sha }}\n      - run: ./cmd\n```\n\n### Incorrect Usage (Label Gates)\n\nThe following workflow uses a Deployment Environment which may be configured to require an approval. However, it check outs the code pointed to by the Pull Request branch reference. At attacker could submit legitimate code for review and then change it once it gets approved.\n\n```yaml\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    if: contains(github.event.pull_request.labels.*.name, 'safe-to-test')\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          ref: ${{ github.event.pull_request.head.ref }}\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n      - run: ./cmd\n```\n\n### Correct Usage (Label Gates)\n\nUse immutable references (Commit SHA) to make sure that the reviewed code does not change between the check and the use.\n\n```yaml\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    if: contains(github.event.pull_request.labels.*.name, 'safe-to-test')\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n      - run: ./cmd\n```\n\n## References\n\n- [ActionsTOCTOU](https://github.com/AdnaneKhan/ActionsTOCTOU).\n","text":"## Overview\n\nUntrusted Checkout is protected by a security check but the checked-out branch can be changed after the check.\n\n## Recommendation\n\nVerify that the code has not been modified after the security check. This may be achieved differently depending on the type of check:\n\n- Deployment Environment Approval: Make sure to use a non-mutable reference to the code to be executed. For example use a `sha` instead of a `ref`.\n- Label Gates: Make sure to use a non-mutable reference to the code to be executed. For example use a `sha` instead of a `ref`.\n\n## Example\n\n### Incorrect Usage (Deployment Environment Approval)\n\nThe following workflow uses a Deployment Environment which may be configured to require an approval. However, it check outs the code pointed to by the Pull Request branch reference. At attacker could submit legitimate code for review and then change it once it gets approved.\n\n```yml\non:\n  pull_request_target:\n    types: [Created]\njobs:\n  test:\n    environment: NeedsApproval\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout from PR branch\n        uses: actions/checkout@v4\n        with:\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n          ref: ${{ github.event.pull_request.head.ref }}\n      - run: ./cmd\n```\n\n### Correct Usage (Deployment Environment Approval)\n\nUse immutable references (Commit SHA) to make sure that the reviewed code does not change between the check and the use.\n\n```yml\non:\n  pull_request_target:\n    types: [Created]\njobs:\n  test:\n    environment: NeedsApproval\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout from PR branch\n        uses: actions/checkout@v4\n        with:\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n          ref: ${{ github.event.pull_request.head.sha }}\n      - run: ./cmd\n```\n\n### Incorrect Usage (Label Gates)\n\nThe following workflow uses a Deployment Environment which may be configured to require an approval. However, it check outs the code pointed to by the Pull Request branch reference. At attacker could submit legitimate code for review and then change it once it gets approved.\n\n```yaml\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    if: contains(github.event.pull_request.labels.*.name, 'safe-to-test')\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          ref: ${{ github.event.pull_request.head.ref }}\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n      - run: ./cmd\n```\n\n### Correct Usage (Label Gates)\n\nUse immutable references (Commit SHA) to make sure that the reviewed code does not change between the check and the use.\n\n```yaml\non:\n  pull_request_target:\n    types: [labeled]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    if: contains(github.event.pull_request.labels.*.name, 'safe-to-test')\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n          repository: ${{ github.event.pull_request.head.repo.full_name }}\n      - run: ./cmd\n```\n\n## References\n\n- [ActionsTOCTOU](https://github.com/AdnaneKhan/ActionsTOCTOU).\n"},"id":"actions/untrusted-checkout-toctou/high","name":"actions/untrusted-checkout-toctou/high","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-367/UntrustedCheckoutTOCTOUHigh.ql","security-severity":"7.5","tags":["actions","external/cwe/cwe-367","security"]},"shortDescription":{"text":"Untrusted Checkout TOCTOU"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"Privileged workflows have read/write access to the base repository and access to secrets. By explicitly checking out and running the build script from a fork the untrusted code is running in an environment that is able to push to the base repository and to access secrets."},"help":{"markdown":"## Overview\n\nGitHub workflows can be triggered through various repository events, including incoming pull requests (PRs) or comments on Issues/PRs. Under certain conditions described below, attackers can take over a repository by opening malicious PRs from forks. The attacks can result in malicious code execution causing unauthorized changes to the repository or exfiltration of repository secrets and a compromise of connected systems.\n\n## Workflow Security Model\n\nIn GitHub Actions, there is a distinction between unprivileged and privileged workflows. For example, a workflow with a `pull_request` trigger is unprivileged while a workflow with `pull_request_target` is privileged.\n\nThis is relevant especially for PRs from forks. Normal PRs can only be submitted by people who have write access to a repository, while PRs from forks can be submitted by anyone.\n\nOn a PR from a fork, an unprivileged `pull_request` workflow has only limited capabilities but a privileged `pull_request_target` workflow is much more dangerous. A privileged workflow:\n\n  * Runs in the context of the base repository\n  * Has access to organization and repository secrets (e.g., API keys, deployment tokens)\n  * Has a read/write `GITHUB_TOKEN` by default\n  * Can access private resources\n\nCertain triggers automatically grant a workflow elevated privileges:\n\n  * `pull_request_target` as described above\n  * `workflow_run`: Triggered when another workflow completes.\n  * `issue_comment`: Triggered when a comment is made on an issue or PR.\n\n## Attack Details\n\n  * A repository has a privileged workflow\n  * An attacker forks the repository and adds malicious code (e.g., in the build script)\n  * The attacker opens a PR from the fork, and, if needed, comments on the PR\n  * The workflow in the base repository checks out the forked code\n  * The workflow runs the malicious code\n\nPlease note that not only build scripts can be malicious code vectors. There is a large number of other possibilities. Some of them are listed in the [LOTP](https://boostsecurityio.github.io/lotp/) catalog.\n\n## Recommendation\n\n- Avoid using `pull_request_target` unless necessary.\n- Employ unprivileged `pull_request` workflows followed by `workflow_run` for privileged operations.\n- Use labels like `safe to test` to vet PRs and manage the execution context appropriately.\n\nThe best practice is to handle the potentially untrusted pull request via the **pull_request** trigger so that it is isolated in an unprivileged environment. The workflow processing the pull request should then store any results like code coverage or failed/passed tests in artifacts and exit. A second privileged workflow with the access to repository secrets, triggered by the completion of the first workflow using `workflow_run` trigger event, downloads the artifacts and make any necessary modifications to the repository or interact with third party services that require repository secrets (e.g. API tokens).\n\nThe artifacts downloaded from the first workflow should be considered untrusted and must be verified.\n\nAdditionally, ensure that least privilege are used both at the workflow level (through event triggers and workflow permissions) and job level (through job permissions).\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow checks-out untrusted code in a privileged context and runs user-controlled code (in this case package.json scripts) which will grant privileged access to the attacker:\n\n```yaml\non: pull_request_target\n\njobs:\n  build:\n    name: Build and test\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n\n      - uses: actions/setup-node@v1\n      - run: |\n          npm install # scripts in package.json from PR would be executed here\n          npm build\n\n      - uses: completely/fakeaction@v2\n        with:\n          arg1: ${{ secrets.supersecret }}\n\n      - uses: fakerepo/comment-on-pr@v1\n        with:\n          message: |\n            Thank you!\n```\n\n### Correct Usage\n\nAn example shows how to use two workflows: one for processing the untrusted PR and the other for using the results in a safe context.\n\n**ReceivePR.yml** (untrusted PR handling with artifact creation):\n\n```yaml\nname: Receive PR\non:\n  pull_request:\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n      - name: Build\n        run: /bin/bash ./build.sh\n      - name: Save PR number\n        run: |\n          mkdir -p ./pr\n          echo ${{ github.event.number }} \u003e ./pr/NR\n      - uses: actions/upload-artifact@v2\n        with:\n          name: pr\n          path: pr/\n```\n\n**CommentPR.yml** (processing artifacts with privileged access):\n\n```yaml\nname: Comment on the pull request\non:\n  workflow_run:\n    workflows: [\"Receive PR\"]\n    types:\n      - completed\njobs:\n  upload:\n    runs-on: ubuntu-latest\n    if: \u003e\n      github.event.workflow_run.event == 'pull_request' \u0026\u0026\n      github.event.workflow_run.conclusion == 'success'\n    steps:\n      - name: \"Download artifact\"\n        uses: actions/github-script@v3.1.0\n        with:\n          script: |\n            var artifacts = await github.actions.listWorkflowRunArtifacts({\n                owner: context.repo.owner,\n                repo: context.repo.repo,\n                run_id: ${{github.event.workflow_run.id }},\n            });\n            var matchArtifact = artifacts.data.artifacts.filter((artifact) =\u003e {\n              return artifact.name == \"pr\";\n            })[0];\n            var download = await github.actions.downloadArtifact({\n                owner: context.repo.owner,\n                repo: context.repo.repo,\n                artifact_id: matchArtifact.id,\n                archive_format: 'zip',\n            });\n            var fs = require('fs');\n            fs.writeFileSync('${{github.workspace}}/pr.zip', Buffer.from(download.data));\n      - run: |\n          mkdir -p tmp\n          unzip -d tmp/ pr.zip\n      - name: \"Comment on PR\"\n        uses: actions/github-script@v3\n        with:\n          github-token: ${{ secrets.GITHUB_TOKEN }}\n          script: |\n            var fs = require('fs');\n            var issue_number = Number(fs.readFileSync('./tmp/NR'));\n            // Verify that the file contains a numeric value\n            const contains_numeric = /\\d/.test(issue_number);\n            if (contains_numeric) {\n                await github.issues.createComment({\n                  owner: context.repo.owner,\n                  repo: context.repo.repo,\n                  issue_number: issue_number,\n                  body: 'Everything is OK. Thank you for the PR!'\n                });\n            }\n```\n\n## References\n\n- GitHub Security Lab Research: [Keeping your GitHub Actions and workflows secure Part 1: Preventing pwn requests](https://securitylab.github.com/research/github-actions-preventing-pwn-requests/).\n- Mitigating risks of untrusted checkout: [GitHub Docs](https://docs.github.com/en/enterprise-cloud@latest/actions/reference/security/secure-use#mitigating-the-risks-of-untrusted-code-checkout).\n- Securing with least privilege: [Workflow secure use](https://docs.github.com/en/actions/reference/security/secure-use).\n- Living Off the Pipeline: [LOTP](https://boostsecurityio.github.io/lotp/).\n","text":"## Overview\n\nGitHub workflows can be triggered through various repository events, including incoming pull requests (PRs) or comments on Issues/PRs. Under certain conditions described below, attackers can take over a repository by opening malicious PRs from forks. The attacks can result in malicious code execution causing unauthorized changes to the repository or exfiltration of repository secrets and a compromise of connected systems.\n\n## Workflow Security Model\n\nIn GitHub Actions, there is a distinction between unprivileged and privileged workflows. For example, a workflow with a `pull_request` trigger is unprivileged while a workflow with `pull_request_target` is privileged.\n\nThis is relevant especially for PRs from forks. Normal PRs can only be submitted by people who have write access to a repository, while PRs from forks can be submitted by anyone.\n\nOn a PR from a fork, an unprivileged `pull_request` workflow has only limited capabilities but a privileged `pull_request_target` workflow is much more dangerous. A privileged workflow:\n\n  * Runs in the context of the base repository\n  * Has access to organization and repository secrets (e.g., API keys, deployment tokens)\n  * Has a read/write `GITHUB_TOKEN` by default\n  * Can access private resources\n\nCertain triggers automatically grant a workflow elevated privileges:\n\n  * `pull_request_target` as described above\n  * `workflow_run`: Triggered when another workflow completes.\n  * `issue_comment`: Triggered when a comment is made on an issue or PR.\n\n## Attack Details\n\n  * A repository has a privileged workflow\n  * An attacker forks the repository and adds malicious code (e.g., in the build script)\n  * The attacker opens a PR from the fork, and, if needed, comments on the PR\n  * The workflow in the base repository checks out the forked code\n  * The workflow runs the malicious code\n\nPlease note that not only build scripts can be malicious code vectors. There is a large number of other possibilities. Some of them are listed in the [LOTP](https://boostsecurityio.github.io/lotp/) catalog.\n\n## Recommendation\n\n- Avoid using `pull_request_target` unless necessary.\n- Employ unprivileged `pull_request` workflows followed by `workflow_run` for privileged operations.\n- Use labels like `safe to test` to vet PRs and manage the execution context appropriately.\n\nThe best practice is to handle the potentially untrusted pull request via the **pull_request** trigger so that it is isolated in an unprivileged environment. The workflow processing the pull request should then store any results like code coverage or failed/passed tests in artifacts and exit. A second privileged workflow with the access to repository secrets, triggered by the completion of the first workflow using `workflow_run` trigger event, downloads the artifacts and make any necessary modifications to the repository or interact with third party services that require repository secrets (e.g. API tokens).\n\nThe artifacts downloaded from the first workflow should be considered untrusted and must be verified.\n\nAdditionally, ensure that least privilege are used both at the workflow level (through event triggers and workflow permissions) and job level (through job permissions).\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow checks-out untrusted code in a privileged context and runs user-controlled code (in this case package.json scripts) which will grant privileged access to the attacker:\n\n```yaml\non: pull_request_target\n\njobs:\n  build:\n    name: Build and test\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n\n      - uses: actions/setup-node@v1\n      - run: |\n          npm install # scripts in package.json from PR would be executed here\n          npm build\n\n      - uses: completely/fakeaction@v2\n        with:\n          arg1: ${{ secrets.supersecret }}\n\n      - uses: fakerepo/comment-on-pr@v1\n        with:\n          message: |\n            Thank you!\n```\n\n### Correct Usage\n\nAn example shows how to use two workflows: one for processing the untrusted PR and the other for using the results in a safe context.\n\n**ReceivePR.yml** (untrusted PR handling with artifact creation):\n\n```yaml\nname: Receive PR\non:\n  pull_request:\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n      - name: Build\n        run: /bin/bash ./build.sh\n      - name: Save PR number\n        run: |\n          mkdir -p ./pr\n          echo ${{ github.event.number }} \u003e ./pr/NR\n      - uses: actions/upload-artifact@v2\n        with:\n          name: pr\n          path: pr/\n```\n\n**CommentPR.yml** (processing artifacts with privileged access):\n\n```yaml\nname: Comment on the pull request\non:\n  workflow_run:\n    workflows: [\"Receive PR\"]\n    types:\n      - completed\njobs:\n  upload:\n    runs-on: ubuntu-latest\n    if: \u003e\n      github.event.workflow_run.event == 'pull_request' \u0026\u0026\n      github.event.workflow_run.conclusion == 'success'\n    steps:\n      - name: \"Download artifact\"\n        uses: actions/github-script@v3.1.0\n        with:\n          script: |\n            var artifacts = await github.actions.listWorkflowRunArtifacts({\n                owner: context.repo.owner,\n                repo: context.repo.repo,\n                run_id: ${{github.event.workflow_run.id }},\n            });\n            var matchArtifact = artifacts.data.artifacts.filter((artifact) =\u003e {\n              return artifact.name == \"pr\";\n            })[0];\n            var download = await github.actions.downloadArtifact({\n                owner: context.repo.owner,\n                repo: context.repo.repo,\n                artifact_id: matchArtifact.id,\n                archive_format: 'zip',\n            });\n            var fs = require('fs');\n            fs.writeFileSync('${{github.workspace}}/pr.zip', Buffer.from(download.data));\n      - run: |\n          mkdir -p tmp\n          unzip -d tmp/ pr.zip\n      - name: \"Comment on PR\"\n        uses: actions/github-script@v3\n        with:\n          github-token: ${{ secrets.GITHUB_TOKEN }}\n          script: |\n            var fs = require('fs');\n            var issue_number = Number(fs.readFileSync('./tmp/NR'));\n            // Verify that the file contains a numeric value\n            const contains_numeric = /\\d/.test(issue_number);\n            if (contains_numeric) {\n                await github.issues.createComment({\n                  owner: context.repo.owner,\n                  repo: context.repo.repo,\n                  issue_number: issue_number,\n                  body: 'Everything is OK. Thank you for the PR!'\n                });\n            }\n```\n\n## References\n\n- GitHub Security Lab Research: [Keeping your GitHub Actions and workflows secure Part 1: Preventing pwn requests](https://securitylab.github.com/research/github-actions-preventing-pwn-requests/).\n- Mitigating risks of untrusted checkout: [GitHub Docs](https://docs.github.com/en/enterprise-cloud@latest/actions/reference/security/secure-use#mitigating-the-risks-of-untrusted-code-checkout).\n- Securing with least privilege: [Workflow secure use](https://docs.github.com/en/actions/reference/security/secure-use).\n- Living Off the Pipeline: [LOTP](https://boostsecurityio.github.io/lotp/).\n"},"id":"actions/untrusted-checkout/critical","name":"actions/untrusted-checkout/critical","properties":{"precision":"very-high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-829/UntrustedCheckoutCritical.ql","security-severity":"9.3","tags":["actions","external/cwe/cwe-829","security"]},"shortDescription":{"text":"Checkout of untrusted code in a privileged context"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"Privileged workflows have read/write access to the base repository and access to secrets. By explicitly checking out and running the build script from a fork the untrusted code is running in an environment that is able to push to the base repository and to access secrets."},"help":{"markdown":"## Overview\n\nGitHub workflows can be triggered through various repository events, including incoming pull requests (PRs) or comments on Issues/PRs. Under certain conditions described below, attackers can take over a repository by opening malicious PRs from forks. The attacks can result in malicious code execution causing unauthorized changes to the repository or exfiltration of repository secrets and a compromise of connected systems.\n\n## Workflow Security Model\n\nIn GitHub Actions, there is a distinction between unprivileged and privileged workflows. For example, a workflow with a `pull_request` trigger is unprivileged while a workflow with `pull_request_target` is privileged.\n\nThis is relevant especially for PRs from forks. Normal PRs can only be submitted by people who have write access to a repository, while PRs from forks can be submitted by anyone.\n\nOn a PR from a fork, an unprivileged `pull_request` workflow has only limited capabilities but a privileged `pull_request_target` workflow is much more dangerous. A privileged workflow:\n\n  * Runs in the context of the base repository\n  * Has access to organization and repository secrets (e.g., API keys, deployment tokens)\n  * Has a read/write `GITHUB_TOKEN` by default\n  * Can access private resources\n\nCertain triggers automatically grant a workflow elevated privileges:\n\n  * `pull_request_target` as described above\n  * `workflow_run`: Triggered when another workflow completes.\n  * `issue_comment`: Triggered when a comment is made on an issue or PR.\n\n## Attack Details\n\n  * A repository has a privileged workflow\n  * An attacker forks the repository and adds malicious code (e.g., in the build script)\n  * The attacker opens a PR from the fork, and, if needed, comments on the PR\n  * The workflow in the base repository checks out the forked code\n  * The workflow runs the malicious code\n\nPlease note that not only build scripts can be malicious code vectors. There is a large number of other possibilities. Some of them are listed in the [LOTP](https://boostsecurityio.github.io/lotp/) catalog.\n\n## Recommendation\n\n- Avoid using `pull_request_target` unless necessary.\n- Employ unprivileged `pull_request` workflows followed by `workflow_run` for privileged operations.\n- Use labels like `safe to test` to vet PRs and manage the execution context appropriately.\n\nThe best practice is to handle the potentially untrusted pull request via the **pull_request** trigger so that it is isolated in an unprivileged environment. The workflow processing the pull request should then store any results like code coverage or failed/passed tests in artifacts and exit. A second privileged workflow with the access to repository secrets, triggered by the completion of the first workflow using `workflow_run` trigger event, downloads the artifacts and make any necessary modifications to the repository or interact with third party services that require repository secrets (e.g. API tokens).\n\nThe artifacts downloaded from the first workflow should be considered untrusted and must be verified.\n\nAdditionally, ensure that least privilege are used both at the workflow level (through event triggers and workflow permissions) and job level (through job permissions).\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow checks-out untrusted code in a privileged context and runs user-controlled code (in this case package.json scripts) which will grant privileged access to the attacker:\n\n```yaml\non: pull_request_target\n\njobs:\n  build:\n    name: Build and test\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n\n      - uses: actions/setup-node@v1\n      - run: |\n          npm install # scripts in package.json from PR would be executed here\n          npm build\n\n      - uses: completely/fakeaction@v2\n        with:\n          arg1: ${{ secrets.supersecret }}\n\n      - uses: fakerepo/comment-on-pr@v1\n        with:\n          message: |\n            Thank you!\n```\n\n### Correct Usage\n\nAn example shows how to use two workflows: one for processing the untrusted PR and the other for using the results in a safe context.\n\n**ReceivePR.yml** (untrusted PR handling with artifact creation):\n\n```yaml\nname: Receive PR\non:\n  pull_request:\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n      - name: Build\n        run: /bin/bash ./build.sh\n      - name: Save PR number\n        run: |\n          mkdir -p ./pr\n          echo ${{ github.event.number }} \u003e ./pr/NR\n      - uses: actions/upload-artifact@v2\n        with:\n          name: pr\n          path: pr/\n```\n\n**CommentPR.yml** (processing artifacts with privileged access):\n\n```yaml\nname: Comment on the pull request\non:\n  workflow_run:\n    workflows: [\"Receive PR\"]\n    types:\n      - completed\njobs:\n  upload:\n    runs-on: ubuntu-latest\n    if: \u003e\n      github.event.workflow_run.event == 'pull_request' \u0026\u0026\n      github.event.workflow_run.conclusion == 'success'\n    steps:\n      - name: \"Download artifact\"\n        uses: actions/github-script@v3.1.0\n        with:\n          script: |\n            var artifacts = await github.actions.listWorkflowRunArtifacts({\n                owner: context.repo.owner,\n                repo: context.repo.repo,\n                run_id: ${{github.event.workflow_run.id }},\n            });\n            var matchArtifact = artifacts.data.artifacts.filter((artifact) =\u003e {\n              return artifact.name == \"pr\";\n            })[0];\n            var download = await github.actions.downloadArtifact({\n                owner: context.repo.owner,\n                repo: context.repo.repo,\n                artifact_id: matchArtifact.id,\n                archive_format: 'zip',\n            });\n            var fs = require('fs');\n            fs.writeFileSync('${{github.workspace}}/pr.zip', Buffer.from(download.data));\n      - run: |\n          mkdir -p tmp\n          unzip -d tmp/ pr.zip\n      - name: \"Comment on PR\"\n        uses: actions/github-script@v3\n        with:\n          github-token: ${{ secrets.GITHUB_TOKEN }}\n          script: |\n            var fs = require('fs');\n            var issue_number = Number(fs.readFileSync('./tmp/NR'));\n            // Verify that the file contains a numeric value\n            const contains_numeric = /\\d/.test(issue_number);\n            if (contains_numeric) {\n                await github.issues.createComment({\n                  owner: context.repo.owner,\n                  repo: context.repo.repo,\n                  issue_number: issue_number,\n                  body: 'Everything is OK. Thank you for the PR!'\n                });\n            }\n```\n\n## References\n\n- GitHub Security Lab Research: [Keeping your GitHub Actions and workflows secure Part 1: Preventing pwn requests](https://securitylab.github.com/research/github-actions-preventing-pwn-requests/).\n- Mitigating risks of untrusted checkout: [GitHub Docs](https://docs.github.com/en/enterprise-cloud@latest/actions/reference/security/secure-use#mitigating-the-risks-of-untrusted-code-checkout).\n- Securing with least privilege: [Workflow secure use](https://docs.github.com/en/actions/reference/security/secure-use).\n- Living Off the Pipeline: [LOTP](https://boostsecurityio.github.io/lotp/).\n","text":"## Overview\n\nGitHub workflows can be triggered through various repository events, including incoming pull requests (PRs) or comments on Issues/PRs. Under certain conditions described below, attackers can take over a repository by opening malicious PRs from forks. The attacks can result in malicious code execution causing unauthorized changes to the repository or exfiltration of repository secrets and a compromise of connected systems.\n\n## Workflow Security Model\n\nIn GitHub Actions, there is a distinction between unprivileged and privileged workflows. For example, a workflow with a `pull_request` trigger is unprivileged while a workflow with `pull_request_target` is privileged.\n\nThis is relevant especially for PRs from forks. Normal PRs can only be submitted by people who have write access to a repository, while PRs from forks can be submitted by anyone.\n\nOn a PR from a fork, an unprivileged `pull_request` workflow has only limited capabilities but a privileged `pull_request_target` workflow is much more dangerous. A privileged workflow:\n\n  * Runs in the context of the base repository\n  * Has access to organization and repository secrets (e.g., API keys, deployment tokens)\n  * Has a read/write `GITHUB_TOKEN` by default\n  * Can access private resources\n\nCertain triggers automatically grant a workflow elevated privileges:\n\n  * `pull_request_target` as described above\n  * `workflow_run`: Triggered when another workflow completes.\n  * `issue_comment`: Triggered when a comment is made on an issue or PR.\n\n## Attack Details\n\n  * A repository has a privileged workflow\n  * An attacker forks the repository and adds malicious code (e.g., in the build script)\n  * The attacker opens a PR from the fork, and, if needed, comments on the PR\n  * The workflow in the base repository checks out the forked code\n  * The workflow runs the malicious code\n\nPlease note that not only build scripts can be malicious code vectors. There is a large number of other possibilities. Some of them are listed in the [LOTP](https://boostsecurityio.github.io/lotp/) catalog.\n\n## Recommendation\n\n- Avoid using `pull_request_target` unless necessary.\n- Employ unprivileged `pull_request` workflows followed by `workflow_run` for privileged operations.\n- Use labels like `safe to test` to vet PRs and manage the execution context appropriately.\n\nThe best practice is to handle the potentially untrusted pull request via the **pull_request** trigger so that it is isolated in an unprivileged environment. The workflow processing the pull request should then store any results like code coverage or failed/passed tests in artifacts and exit. A second privileged workflow with the access to repository secrets, triggered by the completion of the first workflow using `workflow_run` trigger event, downloads the artifacts and make any necessary modifications to the repository or interact with third party services that require repository secrets (e.g. API tokens).\n\nThe artifacts downloaded from the first workflow should be considered untrusted and must be verified.\n\nAdditionally, ensure that least privilege are used both at the workflow level (through event triggers and workflow permissions) and job level (through job permissions).\n\n## Example\n\n### Incorrect Usage\n\nThe following workflow checks-out untrusted code in a privileged context and runs user-controlled code (in this case package.json scripts) which will grant privileged access to the attacker:\n\n```yaml\non: pull_request_target\n\njobs:\n  build:\n    name: Build and test\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n        with:\n          ref: ${{ github.event.pull_request.head.sha }}\n\n      - uses: actions/setup-node@v1\n      - run: |\n          npm install # scripts in package.json from PR would be executed here\n          npm build\n\n      - uses: completely/fakeaction@v2\n        with:\n          arg1: ${{ secrets.supersecret }}\n\n      - uses: fakerepo/comment-on-pr@v1\n        with:\n          message: |\n            Thank you!\n```\n\n### Correct Usage\n\nAn example shows how to use two workflows: one for processing the untrusted PR and the other for using the results in a safe context.\n\n**ReceivePR.yml** (untrusted PR handling with artifact creation):\n\n```yaml\nname: Receive PR\non:\n  pull_request:\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v2\n      - name: Build\n        run: /bin/bash ./build.sh\n      - name: Save PR number\n        run: |\n          mkdir -p ./pr\n          echo ${{ github.event.number }} \u003e ./pr/NR\n      - uses: actions/upload-artifact@v2\n        with:\n          name: pr\n          path: pr/\n```\n\n**CommentPR.yml** (processing artifacts with privileged access):\n\n```yaml\nname: Comment on the pull request\non:\n  workflow_run:\n    workflows: [\"Receive PR\"]\n    types:\n      - completed\njobs:\n  upload:\n    runs-on: ubuntu-latest\n    if: \u003e\n      github.event.workflow_run.event == 'pull_request' \u0026\u0026\n      github.event.workflow_run.conclusion == 'success'\n    steps:\n      - name: \"Download artifact\"\n        uses: actions/github-script@v3.1.0\n        with:\n          script: |\n            var artifacts = await github.actions.listWorkflowRunArtifacts({\n                owner: context.repo.owner,\n                repo: context.repo.repo,\n                run_id: ${{github.event.workflow_run.id }},\n            });\n            var matchArtifact = artifacts.data.artifacts.filter((artifact) =\u003e {\n              return artifact.name == \"pr\";\n            })[0];\n            var download = await github.actions.downloadArtifact({\n                owner: context.repo.owner,\n                repo: context.repo.repo,\n                artifact_id: matchArtifact.id,\n                archive_format: 'zip',\n            });\n            var fs = require('fs');\n            fs.writeFileSync('${{github.workspace}}/pr.zip', Buffer.from(download.data));\n      - run: |\n          mkdir -p tmp\n          unzip -d tmp/ pr.zip\n      - name: \"Comment on PR\"\n        uses: actions/github-script@v3\n        with:\n          github-token: ${{ secrets.GITHUB_TOKEN }}\n          script: |\n            var fs = require('fs');\n            var issue_number = Number(fs.readFileSync('./tmp/NR'));\n            // Verify that the file contains a numeric value\n            const contains_numeric = /\\d/.test(issue_number);\n            if (contains_numeric) {\n                await github.issues.createComment({\n                  owner: context.repo.owner,\n                  repo: context.repo.repo,\n                  issue_number: issue_number,\n                  body: 'Everything is OK. Thank you for the PR!'\n                });\n            }\n```\n\n## References\n\n- GitHub Security Lab Research: [Keeping your GitHub Actions and workflows secure Part 1: Preventing pwn requests](https://securitylab.github.com/research/github-actions-preventing-pwn-requests/).\n- Mitigating risks of untrusted checkout: [GitHub Docs](https://docs.github.com/en/enterprise-cloud@latest/actions/reference/security/secure-use#mitigating-the-risks-of-untrusted-code-checkout).\n- Securing with least privilege: [Workflow secure use](https://docs.github.com/en/actions/reference/security/secure-use).\n- Living Off the Pipeline: [LOTP](https://boostsecurityio.github.io/lotp/).\n"},"id":"actions/untrusted-checkout/high","name":"actions/untrusted-checkout/high","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-829/UntrustedCheckoutHigh.ql","security-severity":"7.5","tags":["actions","external/cwe/cwe-829","security"]},"shortDescription":{"text":"Checkout of untrusted code in a privileged context"}},{"defaultConfiguration":{"level":"error"},"fullDescription":{"text":"The workflow is using an action with known vulnerabilities."},"help":{"markdown":"## Overview\n\nThe security of the workflow and the repository could be compromised by GitHub Actions workflows that utilize GitHub Actions with known vulnerabilities.\n\n## Recommendation\n\nEither remove the component from the workflow or upgrade it to a version that is not vulnerable.\n\n## References\n\n- GitHub Docs: [Keeping your actions up to date with Dependabot](https://docs.github.com/en/code-security/dependabot/working-with-dependabot/keeping-your-actions-up-to-date-with-dependabot).\n","text":"## Overview\n\nThe security of the workflow and the repository could be compromised by GitHub Actions workflows that utilize GitHub Actions with known vulnerabilities.\n\n## Recommendation\n\nEither remove the component from the workflow or upgrade it to a version that is not vulnerable.\n\n## References\n\n- GitHub Docs: [Keeping your actions up to date with Dependabot](https://docs.github.com/en/code-security/dependabot/working-with-dependabot/keeping-your-actions-up-to-date-with-dependabot).\n"},"id":"actions/vulnerable-action","name":"actions/vulnerable-action","properties":{"precision":"high","queryURI":"https://github.com/github/codeql/blob/94101634db019b00637dd5cfe241f7e570da290d/actions/ql/src/Security/CWE-1395/UseOfKnownVulnerableAction.ql","security-severity":"7.5","tags":["actions","external/cwe/cwe-1395","security"]},"shortDescription":{"text":"Use of a known vulnerable action"}}],"semanticVersion":"0.6.32+94101634db019b00637dd5cfe241f7e570da290d"},{"name":"codeql/actions-all","semanticVersion":"0.4.40+94101634db019b00637dd5cfe241f7e570da290d"},{"name":"codeql/javascript-all","semanticVersion":"2.8.2+94101634db019b00637dd5cfe241f7e570da290d"},{"name":"codeql/threat-models","semanticVersion":"1.0.54+94101634db019b00637dd5cfe241f7e570da290d"}]},"versionControlProvenance":[{"branch":"refs/heads/main","repositoryUri":"https://github.com/MoneyCat-inc/otel-ops-pack","revisionId":"fd18a6caa8b843d9b29031e3d6c09c0277c20ecb"}]}],"$schema":"https://raw.githubusercontent.com/oasis-tcs/sarif-spec/master/Schemata/sarif-schema-2.1.0.json","version":"2.1.0"}
