# ❌ BossCat Gate Verification (run #329)

![Badge](../../badges/run-18458364408.svg)

- **ID:** 18458364408
- **Workflow:** BossCat Gate Verification
- **Actor:** @fubumaki
- **Event/Branch:** `push` / `main`
- **SHA:** `789c6cac6246d66bf156fe392b0d78f2a75363b1`
- **Started:** 2025-10-13T07:28:13Z
- **Duration:** 71s
- **Conclusion:** `failure`

## TL;DR
Failed in 71s. 15 failing step(s). Logs archived (0 bytes).

## Failing Steps
- **Gate (prod)** › Run actions/upload-artifact@v4 — `failure`
- **Gate (prod)** › Check consecutive green prod runs (>=3) and propose SBOM_STRICT=true — `skipped`
- **Gate (prod)** › Post Setup pnpm cache — `skipped`
- **Gate (prod)** › Post Setup Node — `skipped`
- **Gate (ci)** › Generate SBOM + Attestation (prod only) — `skipped`
- **Gate (ci)** › Verify SBOM (strict when SBOM_STRICT=true) — `skipped`
- **Gate (ci)** › Run actions/upload-artifact@v4 — `failure`
- **Gate (ci)** › Upload SBOM artifacts (prod only) — `skipped`
- **Gate (ci)** › Check consecutive green prod runs (>=3) and propose SBOM_STRICT=true — `skipped`
- **Gate (ci)** › Post Setup pnpm cache — `skipped`
- **Gate (ci)** › Post Setup Node — `skipped`
- **Gate (stg)** › Generate SBOM + Attestation (prod only) — `skipped`
- **Gate (stg)** › Verify SBOM (strict when SBOM_STRICT=true) — `skipped`
- **Gate (stg)** › Upload SBOM artifacts (prod only) — `skipped`
- **Gate (stg)** › Check consecutive green prod runs (>=3) and propose SBOM_STRICT=true — `skipped`

## Jobs
- Gate (prod) • failure • 30 steps
- Gate (ci) • failure • 30 steps
- Gate (stg) • success • 30 steps

---
<sub>Archived by BossCat Run Conveyor • State machine: QUEUED → ARCHIVING → ARCHIVED → DELETED</sub>
