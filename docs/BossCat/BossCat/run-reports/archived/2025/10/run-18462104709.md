# ✅ BossCat Gate Verification (run #342)

![Badge](../../badges/run-18462104709.svg)

- **ID:** 18462104709
- **Workflow:** BossCat Gate Verification
- **Actor:** @fubumaki
- **Event/Branch:** `push` / `main`
- **SHA:** `f1c49ffff5e00122133d437611fd5f959d51d84e`
- **Started:** 2025-10-13T10:00:47Z
- **Duration:** 86s
- **Conclusion:** `success`

## TL;DR
Passed in 86s. Longest step: Build rhysd/actionlint@v1.7.4 (25s).

## Failing Steps
- **Gate Verify (stg)** › Generate SBOM + Attestation (prod only) — `skipped`
- **Gate Verify (stg)** › Verify SBOM (strict when SBOM_STRICT=true) — `skipped`
- **Gate Verify (stg)** › Archive ICF rollup (timestamped) — `skipped`
- **Gate Verify (stg)** › Run actions/upload-artifact@v4 — `skipped`
- **Gate Verify (stg)** › Upload SBOM artifacts (prod only) — `skipped`
- **Gate Verify (stg)** › Check consecutive green prod runs (>=3) and propose SBOM_STRICT=true — `skipped`
- **Gate Verify (local)** › Generate SBOM + Attestation (prod only) — `skipped`
- **Gate Verify (local)** › Verify SBOM (strict when SBOM_STRICT=true) — `skipped`
- **Gate Verify (local)** › Archive ICF rollup (timestamped) — `skipped`
- **Gate Verify (local)** › Run actions/upload-artifact@v4 — `skipped`
- **Gate Verify (local)** › Upload SBOM artifacts (prod only) — `skipped`
- **Gate Verify (local)** › Check consecutive green prod runs (>=3) and propose SBOM_STRICT=true — `skipped`
- **Gate Verify (ci)** › Generate SBOM + Attestation (prod only) — `skipped`
- **Gate Verify (ci)** › Verify SBOM (strict when SBOM_STRICT=true) — `skipped`
- **Gate Verify (ci)** › Upload SBOM artifacts (prod only) — `skipped`
- **Gate Verify (ci)** › Check consecutive green prod runs (>=3) and propose SBOM_STRICT=true — `skipped`
- **Gate Verify (prod)** › Archive ICF rollup (timestamped) — `skipped`
- **Gate Verify (prod)** › Run actions/upload-artifact@v4 — `skipped`

## Jobs
- Gate Verify (stg) • success • 30 steps
- Gate Verify (local) • success • 30 steps
- Gate Verify (ci) • success • 30 steps
- Gate Verify (prod) • success • 30 steps

---
<sub>Archived by BossCat Run Conveyor • State machine: QUEUED → ARCHIVING → ARCHIVED → DELETED</sub>
