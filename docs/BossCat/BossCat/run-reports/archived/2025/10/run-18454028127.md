# ❌ BossCat Regression Matrix (run #2)

![Badge](../../badges/run-18454028127.svg)

- **ID:** 18454028127
- **Workflow:** BossCat Regression Matrix
- **Actor:** @fubumaki
- **Event/Branch:** `schedule` / `main`
- **SHA:** `a8c45991274eee672d95e5544a78847802b7587c`
- **Started:** 2025-10-13T03:14:11Z
- **Duration:** 51s
- **Conclusion:** `failure`

## TL;DR
Failed in 51s. 90 failing step(s). Logs archived (0 bytes).

## Failing Steps
- **Regression (GATE_007 @ local)** › Set up Node and pnpm — `failure`
- **Regression (GATE_007 @ local)** › Enable corepack and install deps — `skipped`
- **Regression (GATE_007 @ local)** › Prepare directories — `skipped`
- **Regression (GATE_007 @ local)** › Run IONA Gate Verification — `skipped`
- **Regression (GATE_007 @ local)** › GPU_FIX lane (Option B) — `skipped`
- **Regression (GATE_007 @ local)** › Upload Nexploit SARIF (if present) — `skipped`
- **Regression (GATE_007 @ local)** › Nexploit Gate (fail on Critical/High) — `skipped`
- **Regression (GATE_007 @ local)** › Post Set up Node and pnpm — `skipped`
- **Regression (GATE_007 @ local)** › Post Cache pip — `skipped`
- **Regression (GATE_007 @ local)** › Post Set up Python — `skipped`
- **Regression (GATE_006 @ local)** › Set up Node and pnpm — `failure`
- **Regression (GATE_006 @ local)** › Enable corepack and install deps — `skipped`
- **Regression (GATE_006 @ local)** › Prepare directories — `skipped`
- **Regression (GATE_006 @ local)** › Run IONA Gate Verification — `skipped`
- **Regression (GATE_006 @ local)** › GPU_FIX lane (Option B) — `skipped`
- **Regression (GATE_006 @ local)** › Upload Nexploit SARIF (if present) — `skipped`
- **Regression (GATE_006 @ local)** › Nexploit Gate (fail on Critical/High) — `skipped`
- **Regression (GATE_006 @ local)** › Post Set up Node and pnpm — `skipped`
- **Regression (GATE_006 @ local)** › Post Cache pip — `skipped`
- **Regression (GATE_006 @ local)** › Post Set up Python — `skipped`
- **Regression (GATE_006 @ ci)** › Set up Node and pnpm — `failure`
- **Regression (GATE_006 @ ci)** › Enable corepack and install deps — `skipped`
- **Regression (GATE_006 @ ci)** › Prepare directories — `skipped`
- **Regression (GATE_006 @ ci)** › Run IONA Gate Verification — `skipped`
- **Regression (GATE_006 @ ci)** › GPU_FIX lane (Option B) — `skipped`
- **Regression (GATE_006 @ ci)** › Upload Nexploit SARIF (if present) — `skipped`
- **Regression (GATE_006 @ ci)** › Nexploit Gate (fail on Critical/High) — `skipped`
- **Regression (GATE_006 @ ci)** › Post Set up Node and pnpm — `skipped`
- **Regression (GATE_006 @ ci)** › Post Cache pip — `skipped`
- **Regression (GATE_006 @ ci)** › Post Set up Python — `skipped`
- **Regression (IONA @ local)** › Set up Node and pnpm — `failure`
- **Regression (IONA @ local)** › Enable corepack and install deps — `skipped`
- **Regression (IONA @ local)** › Prepare directories — `skipped`
- **Regression (IONA @ local)** › Run IONA Gate Verification — `skipped`
- **Regression (IONA @ local)** › GPU_FIX lane (Option B) — `skipped`
- **Regression (IONA @ local)** › Upload Nexploit SARIF (if present) — `skipped`
- **Regression (IONA @ local)** › Nexploit Gate (fail on Critical/High) — `skipped`
- **Regression (IONA @ local)** › Post Set up Node and pnpm — `skipped`
- **Regression (IONA @ local)** › Post Cache pip — `skipped`
- **Regression (IONA @ local)** › Post Set up Python — `skipped`
- **Regression (IONA @ ci)** › Set up Node and pnpm — `failure`
- **Regression (IONA @ ci)** › Enable corepack and install deps — `skipped`
- **Regression (IONA @ ci)** › Prepare directories — `skipped`
- **Regression (IONA @ ci)** › Run IONA Gate Verification — `skipped`
- **Regression (IONA @ ci)** › GPU_FIX lane (Option B) — `skipped`
- **Regression (IONA @ ci)** › Upload Nexploit SARIF (if present) — `skipped`
- **Regression (IONA @ ci)** › Nexploit Gate (fail on Critical/High) — `skipped`
- **Regression (IONA @ ci)** › Post Set up Node and pnpm — `skipped`
- **Regression (IONA @ ci)** › Post Cache pip — `skipped`
- **Regression (IONA @ ci)** › Post Set up Python — `skipped`
- **Regression (GATE_007 @ ci)** › Set up Node and pnpm — `failure`
- **Regression (GATE_007 @ ci)** › Enable corepack and install deps — `skipped`
- **Regression (GATE_007 @ ci)** › Prepare directories — `skipped`
- **Regression (GATE_007 @ ci)** › Run IONA Gate Verification — `skipped`
- **Regression (GATE_007 @ ci)** › GPU_FIX lane (Option B) — `skipped`
- **Regression (GATE_007 @ ci)** › Upload Nexploit SARIF (if present) — `skipped`
- **Regression (GATE_007 @ ci)** › Nexploit Gate (fail on Critical/High) — `skipped`
- **Regression (GATE_007 @ ci)** › Post Set up Node and pnpm — `skipped`
- **Regression (GATE_007 @ ci)** › Post Cache pip — `skipped`
- **Regression (GATE_007 @ ci)** › Post Set up Python — `skipped`
- **Regression (GATE_007 @ prod)** › Set up Node and pnpm — `failure`
- **Regression (GATE_007 @ prod)** › Enable corepack and install deps — `skipped`
- **Regression (GATE_007 @ prod)** › Prepare directories — `skipped`
- **Regression (GATE_007 @ prod)** › Run IONA Gate Verification — `skipped`
- **Regression (GATE_007 @ prod)** › GPU_FIX lane (Option B) — `skipped`
- **Regression (GATE_007 @ prod)** › Upload Nexploit SARIF (if present) — `skipped`
- **Regression (GATE_007 @ prod)** › Nexploit Gate (fail on Critical/High) — `skipped`
- **Regression (GATE_007 @ prod)** › Post Set up Node and pnpm — `skipped`
- **Regression (GATE_007 @ prod)** › Post Cache pip — `skipped`
- **Regression (GATE_007 @ prod)** › Post Set up Python — `skipped`
- **Regression (GATE_006 @ prod)** › Set up Node and pnpm — `failure`
- **Regression (GATE_006 @ prod)** › Enable corepack and install deps — `skipped`
- **Regression (GATE_006 @ prod)** › Prepare directories — `skipped`
- **Regression (GATE_006 @ prod)** › Run IONA Gate Verification — `skipped`
- **Regression (GATE_006 @ prod)** › GPU_FIX lane (Option B) — `skipped`
- **Regression (GATE_006 @ prod)** › Upload Nexploit SARIF (if present) — `skipped`
- **Regression (GATE_006 @ prod)** › Nexploit Gate (fail on Critical/High) — `skipped`
- **Regression (GATE_006 @ prod)** › Post Set up Node and pnpm — `skipped`
- **Regression (GATE_006 @ prod)** › Post Cache pip — `skipped`
- **Regression (GATE_006 @ prod)** › Post Set up Python — `skipped`
- **Regression (IONA @ prod)** › Set up Node and pnpm — `failure`
- **Regression (IONA @ prod)** › Enable corepack and install deps — `skipped`
- **Regression (IONA @ prod)** › Prepare directories — `skipped`
- **Regression (IONA @ prod)** › Run IONA Gate Verification — `skipped`
- **Regression (IONA @ prod)** › GPU_FIX lane (Option B) — `skipped`
- **Regression (IONA @ prod)** › Upload Nexploit SARIF (if present) — `skipped`
- **Regression (IONA @ prod)** › Nexploit Gate (fail on Critical/High) — `skipped`
- **Regression (IONA @ prod)** › Post Set up Node and pnpm — `skipped`
- **Regression (IONA @ prod)** › Post Cache pip — `skipped`
- **Regression (IONA @ prod)** › Post Set up Python — `skipped`

## Jobs
- Regression (GATE_007 @ local) • failure • 19 steps
- Regression (GATE_006 @ local) • failure • 19 steps
- Regression (GATE_006 @ ci) • failure • 19 steps
- Regression (IONA @ local) • failure • 19 steps
- Regression (IONA @ ci) • failure • 19 steps
- Regression (GATE_007 @ ci) • failure • 19 steps
- Regression (GATE_007 @ prod) • failure • 19 steps
- Regression (GATE_006 @ prod) • failure • 19 steps
- Regression (IONA @ prod) • failure • 19 steps

---
<sub>Archived by BossCat Run Conveyor • State machine: QUEUED → ARCHIVING → ARCHIVED → DELETED</sub>
